# 作業計画 — 精度向上の打ち切り後（2026-08-06）

作成: 2026-08-06 / 担当: Claude Code
前版: `docs/PLAN_BACKCAST_2026-08-06.md`（精度向上の打ち切りにより改訂）

---

## 1. 精度向上を打ち切る（決定）

### 到達点

| 指標 | 値 |
|---|---|
| 166問 要素カバー率 | **0.6462**（420/650） |
| 真の捏造 | **1件**（Q158） |
| 防衛白書30問 | 27/30 |
| TTFT 最大 | 2.65秒 |

**この値を現行 v1.2.9 の確定値として記録する。**

### 試して駄目だったこと

| 経路 | 内容 | 結果 |
|---|---|---|
| **A** | クッション文数 12/16/24 | +3.08pt が上限。**カバー率が上がる条件は必ず捏造が増える** |
| **B** | 索引時の仮想質問生成（Doc2Query--） | **−2.31pt、捏造3倍**。フィルタ込みでも悪化 |

**一貫した結論**: この構成では、**文脈の出し入れで精度は上がらない**。
C4/C5 が上がると C1/C2 が下がり、総量は動かない。

### 残した経路C（構造抽出）に着手しない理由

- 経路A・B が示した壁（文脈の出し入れでは動かない）に、C も当たる可能性がある
- **再 embed を伴い、工数が一段大きい**（数日規模）
- 現行 0.646 でも製品は出荷済みで、顧客要件（非中国系・TTFT 8秒）は満たしている

**打ち切りであって、否定ではない。** 再開したくなったときのために
`docs/QUALITY_ROADMAP_2026-07-26.md` と本計画を残す。

---

## 2. この作業で残った資産（捨てないもの）

| 資産 | 内容 |
|---|---|
| **捏造判定の修理** | 誤検出 **91% → 0%**。これが無いと改善の効果を判定できなかった |
| 棄権判定の修理 | 欠陥3件。`_eval_common.is_unknown()` に一本化 |
| **弱点の所在** | C5 推論 0.465 / C4 要約 0.570 が最弱 |
| **経路A/B の実測** | 次に誰かが同じ手を試そうとしたとき、無駄を避けられる |
| 評価基盤 | 166問・1,044要素を1runで回せる。判定まで自動 |

**次に改善を試すときは、その効果を正しく測れる状態になっている。**

---

## 3. 次にやること（優先順）

### 3-1. Linux 版の構成乖離 — 🔴 **ユーザー判断により保留**（2026-08-06）

**「今は放置でいい」との指示により着手しない。** ただし事実は残す。

**現状が製品として不整合。**

| | Windows v1.2.9 | Linux v1.1.2（未公開） |
|---|---|---|
| LLM | granite4.1:8b | **gemma4:12b** |
| 埋め込み | granite-embedding | **bge-m3（中国系）** |
| リランカー | japanese-reranker-xsmall-v2 | 同左 |

**Linux 版は「非中国系」を満たしていない。** 導入先が RHEL 9 である以上、
Windows だけ達成していても要件を満たしたことにならない。

**再開する場合の手順**（半日）:
1. `linux-native` の設定を granite 構成へ（30分）
2. モデルを Linux 配布物へ同梱（1時間）
3. ビルド・分割・結合実証（2時間）
4. 公開前レビュー（30分）
5. Release 公開（★要承認）

🔴 **保留中に留意すること**: 現在公開中の Linux 版は `linux-v1.1.1` で、
LLM が gemma4:12b、埋め込みが bge-m3（中国系）。**顧客に「非中国系」と
説明する場合、それは Windows 版の話であって Linux 版には当てはまらない。**

### 3-2. v1.2.9 のインストール通し実行

ビルドと結合は検証済みだが、**実際にインストールを完走させた記録が無い**。
稼働中の製品が v1.2.5 なので、実行すると置き換わる。**要承認。**

### 3-3. 8GB 実機の `nvidia-smi` 取得（ユーザー作業）

VRAM 適合が最後まで未検証。**メール1往復で解消する。**

### 3-4. 出荷中の捏造 Q158（保留のまま）

C6 の 13/14 は正常に棄権しており、1問のために棄権を強化すると
**過剰拒否のリスク**がある。既知欠陥として記録し、着手しない。

---

## 4. やらないこと（明示）

| | 理由 |
|---|---|
| 経路C（構造抽出） | 上記のとおり打ち切り |
| クッション/topN の追加探索 | 経路A で行き止まりを実証済み |
| 仮想質問の再挑戦 | 経路B で悪化を実証済み |
| `[P]` 173要素の判定整備 | カバー率の分母が増えるだけで、改善そのものではない |

---

## 5. 見積り

| # | 所要 | 承認 | 状態 |
|---|---|---|---|
| 3-1（Linux 版の統一） | 半日 | 公開時のみ | 🔴 **保留**（ユーザー判断） |
| 3-2（インストール検証） | 1時間 | **要承認**（実環境を変更） | 未着手 |
| 3-3（実機情報） | — | ユーザー作業 | 未依頼 |

---

## 6. 現在、着手待ちの作業は無い

精度向上は打ち切り、Linux 版は保留、残りはいずれも**承認またはユーザー作業**が要る。

**次の指示を待つ状態。** 再開しやすいよう、以下を残してある。

| 残したもの | 場所 |
|---|---|
| 精度向上の全記録（経路A/B の実測） | `docs/WORKLOG.md`、`out/cushion*`、`out/doc2query/` |
| 修理した評価基盤 | `scripts/complex-eval.py`、`scripts/_eval_common.py` |
| 確定値と弱点の所在 | 本文書 §1 |
| 撤回した数値の一覧 | `docs/WORKLOG.md` |
