# WORKLOG — 作業の時系列記録（追記専用）

**このファイルは追記専用。過去のエントリは書き換えない。**
誤りが判明した場合も、古い記録を消さずに新しいエントリで訂正する
（「いつ何を信じていたか」が追えなくなると、同じ誤りを繰り返すため）。

## 読み方・書き方

| | `docs/HANDOFF.md` | `docs/WORKLOG.md`（本ファイル） |
|---|---|---|
| 形式 | 毎回**書き換える** | **追記のみ** |
| 内容 | 今どこにいるか・次に何をするか | 何を試し、何が失敗し、なぜそうしたか |
| 見るとき | セッション開始時にまず読む | 「なぜこうなっているのか」を遡るとき |

**セッション開始時は `HANDOFF.md` → 本ファイルの上から数エントリ、の順で読む。**

### 追記のしかた

```bash
scripts/worklog.sh "見出し" <<'EOF'
本文（markdown）
EOF
```

日時・ブランチ・git HEAD・未コミットの有無は自動で入る。

### 何を書くか

- **失敗と、その原因**（最重要。成功だけ書くと同じ失敗を繰り返す）
- 判断とその根拠（特に「やらないと決めたこと」とその理由）
- 撤回した数値・主張（撤回したことを明記する。後から誰かが再び引用するため）
- 外部に出したもの（Release 公開など、取り消せない操作）

### 何を書かなくてよいか

- コードの構造やコミット履歴から読み取れること
- 途中経過の細かい試行錯誤（結論と原因があればよい）

---

<!-- 新しい記録はこの行のすぐ下に入る -->

## 2026-08-06 11:47 — v1.2.10 をビルド・分割・結合検証まで完了（未公開）

`main` @ `bf92a70`

欠陥2件の修正を入れた v1.2.10 をビルドした。**まだ公開していない。**

## ビルドまでに詰まった点（次回のため記録）

`export-windows.ps1` の必須引数と前提が揃っておらず、4回失敗してから通った。

| 失敗 | 原因 | 対処 |
|---|---|---|
| 1 | 必須引数6つ（SourceDir/NodeDir/OllamaDir/WinSWExe/ModelsDir/RerankerModelDir）未指定 | 全指定 |
| 2 | `-RerankerModelDir` が1階層浅い | `build-deps\reranker\japanese-reranker-xsmall-v2` |
| 3 | `-OutputDir` 既定が相対 `.\dist`、cmd の cwd が `C:\Windows` | 絶対パス指定 |
| 4 | `docs\customer-windows` がビルド木に無い | リポジトリから同期 |

**ビルド元 `C:\LocalRAG\src` はリポジトリとは別コピーで、git 管理外。**
修正はリポジトリ側だけでなくビルド元にも反映する必要がある（今回 collector の修正を手で複写した）。
`windows-native/*.ps1` と `config/` も同様にビルド木へ同期した。

## 検証済み（v1.2.10）

- 修正3件が配布物に入っていること: zip 内エントリまで確認
  （`app/collector/storage/tmp/.placeholder` が zip に存在）
- `install.ps1` の構文: PowerShell パーサで検証 OK
- 分割 5パート → 結合（顧客経路 `Join-OTE-RAG.cmd`）: ハッシュ一致
- zip サイズ 7,860,869,520 バイト
- `.env` 生成の単体試験: 修正前 20/31 件 → 修正後 31/31 件（失われた設定 0）

## 未実施

- 実機インストール（ユーザー操作待ち。UAC のため Claude 側から実行できない）
- インストール後の `.env` 31件の実地確認、collector 安定稼働、RAG 応答と出典
- GitHub Release 公開、v1.2.8/v1.2.9 の取り下げ

## 2026-08-06 11:14 — v1.2.9 のインストール検証で致命的欠陥2件を発見・修正

`main` @ `58a4ec9` / 未コミットの変更あり

実機インストール検証（C:\LocalRAGProd、ポート3005）で、**公開中の v1.2.9 は RAG が全く動かない**ことが判明した。

## 欠陥1: .env の設定 31件中 11件が無言で無効化される（致命的）

`install.ps1` の `Render-Template` が `Get-Content -Raw` を**エンコーディング無指定**で使っていた。
Windows PowerShell 5.1 は BOM 無し UTF-8 をシステム ANSI コードページ（日本語環境では CP932）として読む。
テンプレートの日本語コメント行末の「。」= `E3 80 82` の `82` が CP932 の先頭バイトと解釈され、
**直後の LF を巻き込んで消す**。結果、次行の設定がコメントの一部になり無効化される。

失われていた設定（実測。修正前20件 / 修正後31件で再現確認済み）:
OLLAMA_MODEL_PREF, EMBEDDING_MODEL_PREF, LANCE_HYBRID_RERANK, LANCE_SENTENCE_CUSHION,
RERANKER_QUANTIZED, RERANKER_MODEL_PREF, OLLAMA_DISABLE_THINKING, QUERY_REFORMULATION,
HF_HUB_OFFLINE, TARGET_OCR_LANG, LOCAL_SERVICE_CONTROL

症状は `No embedding model was set.` で埋め込みが0秒で失敗し、検索も回答も成立しない。
**これまでの精度改善（文抽出クッション、ハイブリッド再順位付け等）は出荷物では一切効いていなかった。**

対処:
- `Get-Content $templatePath -Raw -Encoding UTF8` に変更
- 生成後にテンプレートのキー集合と生成物のキー集合を突き合わせ、**1件でも欠けたらインストールを中止**する検証を追加（この種の欠落は無言で進むため）

## 欠陥2: collector が起動時にクラッシュループ

配布物に `app\collector\storage\` が含まれておらず（zip が空ディレクトリを保持しない）、
`collector/utils/files/index.js` の `if (err) resolve();` に **return が無い**ため
`files` が undefined のまま反復に進み `TypeError: files is not iterable` で即死。
WinSW が10秒ごとに再起動する無限ループになる。**顧客の手元では復旧できない。**

対処:
- `return resolve();` に修正（2箇所）。`!fs.existsSync` の分岐も同様に修正
- `storage/tmp` を起動時に自動生成
- `export-windows.ps1` で `storage/tmp/.placeholder` を同梱

## 検証で確認できたこと

- 5分割の結合（顧客経路 `Join-OTE-RAG.cmd`）: ハッシュ一致
- 旧版 v1.2.5 の自動アンインストール: 正常。データは `uninstalled-20260806-093727` へ退避
- モデル取り込み 14GB: granite4.1:8b + granite-embedding:278m
- API 応答: `{"online":true}`

## 併せて見つけた製品欠陥（未修正）

`install.ps1:132` のロールバックが `Remove-Item -Recurse -Force $InstallRoot` を実行する。
コメントには "created by this run" とあるが**このrunが作ったか確認していない**。
顧客が既存の非空フォルダをインストール先に選び失敗すると、**そのフォルダごと消える**。

## 2026-08-06 06:20 — 精度向上を打ち切り — 現行 0.646 を確定値として記録

`main` @ `22a4096` / 未コミットの変更あり

### 決定: **精度向上を一旦打ち切る。** 現行 v1.2.9 の実力を確定値とする。

| 指標 | 確定値 |
|---|---|
| 166問 要素カバー率 | **0.6462**（420/650） |
| 真の捏造 | **1件**（Q158） |
| 防衛白書30問 | 27/30 |
| TTFT 最大 | 2.65秒 |

カテゴリ別: C2 0.763 / C6 0.727 / C1 0.625 / C3 0.616 / C4 0.570 / **C5 0.465**

### 試して駄目だった2経路

| 経路 | 結果 |
|---|---|
| A クッション文数 12/16/24 | +3.08pt が上限。**カバー率が上がる条件は必ず捏造が増える** |
| B 索引時の仮想質問生成 | **−2.31pt、捏造 1→3件**。フィルタ込みでも悪化 |

**一貫した結論: この構成では文脈の出し入れで精度は上がらない。**
C4/C5 が上がると C1/C2 が下がり、総量が動かない。

### 経路C（構造抽出）に進まない理由

- A・B が示した壁に C も当たる可能性がある
- 再 embed を伴い工数が一段大きい（数日規模）
- 現行 0.646 でも顧客要件（非中国系・TTFT 8秒）は満たしている

**打ち切りであって否定ではない。** 再開時は QUALITY_ROADMAP と本記録が出発点。

### 🔴 捨てない資産

- **捏造判定の修理（誤検出 91%→0%）** — これが無いと改善の効果を判定できない
- 棄権判定の修理（欠陥3件）
- 弱点の所在（C5 0.465 / C4 0.570 が最弱）
- **経路A/B の実測** — 次に同じ手を試そうとする人の無駄を防ぐ

### 次の最優先: Linux 版の構成乖離

| | Windows v1.2.9 | Linux v1.1.2（未公開） |
|---|---|---|
| LLM | granite4.1:8b | **gemma4:12b** |
| 埋め込み | granite-embedding | **bge-m3（中国系）** |

**Linux 版は「非中国系」を満たしていない。** 導入先が RHEL 9 である以上、
Windows だけ達成しても要件を満たしたことにならない。半日で解消できる。

## 2026-08-06 06:05 — 経路B（仮想質問生成）を打ち切り — 悪化した

`main` @ `48b11e7` / 未コミットの変更あり

### 判定: **不採用。カバー率が下がり捏造が3倍になった。**

| | 対照 | 仮想質問あり | 差 |
|---|---|---|---|
| 166問 要素カバー率 | 0.6462 | **0.6231** | **−2.31pt** |
| 真の捏造 | 1件 | **3件**（Q01/Q07/Q158） | **+2** |
| C5 推論 | 0.465 | 0.419 | −4.7pt |
| C3 比較 | 0.616 | 0.572 | −4.3pt |

全カテゴリで悪化（C1 のみ +0.7pt）。フィルタ（残存57.2%）は入れた上での結果。

### 🔴 仮説そのものが誤りだった

「文脈の量を変えなければ捏造は増えない」と考えて経路B を選んだが、**増えた**。
質問文をチャンク先頭に足すと**チャンクの意味が薄まり、検索も生成も悪化する**。

一次調査（Doc2Query--、有効性 最大+16%）は英語ベンチの値であり、
**日本語・この文書・この構成では再現しなかった。**

### 事前予想との照合（すべて外れ）

| 予想 | 結果 |
|---|---|
| +2〜4pt | **−2.31pt** |
| P1 は五分五分 | ❌ |
| 捏造は増えない | **❌ 1→3件** |

### 🔴 測定が一度無効になった（原因と教訓）

最初の測定は166問すべての回答が1文字も違わず、**注入が効いていなかった**。
原因は**索引時のチャンクに既に `<document_metadata>` ヘッダが付いていた**こと。
「ヘッダは後段で付く」という思い込みでキーが一度も一致しなかった。

キー計算の言語差・キャッシュ再利用・コードの存在確認は**すべて「正常」と出た**。
診断ログを入れて初めて分かった。**思い込みの外側は測らないと見えない。**

この型（ID や本文の実体が想定と違う）は本作業で3回目:
  1. vector-cache の id と vector-search の id が別空間
  2. 再embed で id が振り直される
  3. チャンクに既にヘッダが付いている

### ゴールに対する現在地

```
G1 カバー率 0.646 → 0.70 以上
  経路A（文脈量）    打ち切り。+3.08pt が上限、必ず捏造が増える
  経路B（仮想質問）  打ち切り。−2.31pt、捏造3倍
  経路C（構造抽出）  未着手。工数大
```

**+5pt に到達する道は、いまのところ見つかっていない。**

### 所要（記録）

生成45分 + フィルタ111分 + 再embed 3.5分 + 評価30分 + 判定30分
＋ 無効測定のやり直し分。**約5時間。**

## 2026-08-06 00:40 — 経路A（文脈量の調整）を打ち切り — 3条件すべてで G1 未達

`main` @ `d58b110` / 未コミットの変更あり

### 判定: **経路A（クッション/topN の調整）では G1（+5pt）に届かない。打ち切る。**

修理後の判定器で166問を測定した全体像:

| TOPK | カバー率 | 差 | 真の捏造 | 判定 |
|---|---|---|---|---|
| **8（現行）** | 0.6462 | — | 1件 | — |
| 12 | 0.6462 | ±0.00pt | 1件 | ❌ P1 |
| 16 | 0.6769 | +3.08pt | **2件** | ❌ **P2違反** |
| 24 | 0.6754 | +2.92pt | **2件** | ❌ **P2違反** |

### 決定的な事実

**カバー率が上がる条件（16・24）は、いずれも捏造が 1→2件に増える。**
3条件すべてで一貫しており、最大でも +3.08pt で G1 の +5pt に届かない。

### 構造的な知見: 文脈量の効き方はカテゴリで逆向き

| TOPK | C1 集約 | C2 マルチホップ | C4 要約 | C5 推論 |
|---|---|---|---|---|
| 12 | **−4.4** | **−5.8** | +5.8 | +11.6 |
| 16 | ±0.0 | +1.1 | **+9.9** | +9.3 |
| 24 | +2.2 | −1.6 | +8.3 | +11.6 |

**一律の値では全体を上げられない。** C4/C5（広く集める設問）は文脈が多いほど良く、
C1/C2（構造を辿る設問）は多いと埋もれる。

なお **C5 の改善は 12 の時点で +11.6pt に達し、24 まで増やしても伸びない**。
文脈量には効く範囲があり、それを超えると捏造だけが増える。

### 事前予想との照合

| 予想 | 結果 |
|---|---|
| 12 も 16 も P1 を満たさない | **半分外れ。16 は P1 を満たした**（+3.08pt） |
| 捏造は 12 で1件、16 で1〜2件 | 的中 |
| どちらも採用に至らず8据え置き | 的中（16 は P2 で落ちた） |

### 🔴 仮説の訂正: C5 はプロンプトの問題ではなかった

「プロンプトが推測を禁じているせいで C5 が低い」と仮説を立てたが、**誤り**。
C5 の要素は推論ではなく**文書の事実**（航空宇宙自衛隊への改編、きらめき2号の
運用終了年度など）。C5 が低いのはその事実が LLM に届いていないためで、
文脈量を増やすと改善する（+11.6pt）ことがそれを裏づける。
**プロンプト調整（当初のステップ3-b）は取り下げる。**

### 次: 経路B — 索引時の仮想質問生成

`docs/RESEARCH_INFERENCE_ABSTENTION_2026-07-28.md` §A-5 が本命候補として記録済み。

- Doc2Query--（arXiv:2301.03266）: 有効性 最大+16%、実行時間−23%、索引−33%
- **応答時間コストがゼロ**（索引時にバッチ処理。G3 を脅かさない）
- **文脈量を変えずに引き当てやすさを上げる**ので、今回判明した
  「増やすと捏造が増える」ジレンマを回避できる
- 🔴 **反証込みの知見: 「生成質問を全部入れると悪化する。フィルタ必須」**

## 2026-08-05 23:20 — [X]判定を修理（誤検出91%→0%）— ステップ1完了

`main` @ `80c6072` / 未コミットの変更あり

### 結果: **測定器が直った。捏造を信頼できる指標で測れるようになった。**

| # | 基準 | 結果 |
|---|---|---|
| P1 | 既知の誤検出の救済 | ✅ **11/11** |
| P2 | 真の捏造の検出 | ✅ **2/2**（Q158・Q139） |
| P3 | 既存の回帰テスト | ✅ 25 passed |
| P4 | 変異注入で検出力 | ✅（壊すと 5→8件に悪化） |
| P5 | 再採点で件数が説明できる | ✅ TOPK=8 →1件 / TOPK=24 →2件 |

### 🔴 撤回した数値（再び引用しないこと）

| 撤回したもの | 正しい値 |
|---|---|
| 「TOPK=24 で捏造が 1→3件」 | **1→2件**。Q17 は誤検出だった |
| 「Q17 は真の捏造」 | **誤検出**。回答は文書どおり（きらめき2号の運用終了年度であって、次期衛星の運用開始年度ではない） |
| 「[X] の真の捏造は Q158 の1件のみ」（fa86df9） | TOPK=8 では1件だが、**TOPK=24 では Q139 も真の捏造** |

### 方式（PDCA 4サイクル）

1. 機械的ガードのみ → 予想8/10・実測**3/10** ❌
2. + LLM二次判定 → 予想9/10・実測**8/10** ❌
3. + 原典(gold_quotes)を渡す → **P2 が壊れた** ❌ **撤回**
4. + 正規表現の個別修正2件 → **10/10 ✅**

**3サイクル目の教訓**: 「文書に無いこと」を検出する判定に文書の抜粋を渡すと、
judge が「原典に近い数値がある」と見て**真の捏造を見逃す**。

### 実装

- `complex-eval.py`: 誤検出ガード2つ（否定語尾・文境界）
- `complex-eval.py`: `[X]` の二次判定。決定的判定を一次フィルタにし、
  **発火した件だけ** LLM が3回判定して多数決。割れたら人手確認（自動で「捏造なし」と決めない）
- fixture: **164個中2個のみ**修正（Q038 は正しい値191にも一致、Q092 は「米国防省とNATOはそれぞれ」に誤爆）
- 判定モデルは `gemma4:12b`（被判定側 granite4.1:8b と別にして自己採点を回避）

### 🔴 自戒: 人手の目視判定も同じくらい誤る

今日、**「捏造だ」と判断したもののうち11件が誤検出**だった（C3の8件＋Q17ほか）。
機械の判定を疑うのと同じだけ、自分の目視も疑う必要がある。
だからこそ**機械的に再現可能な基準**を持つ意味がある。

### 残る限界

- `[P]`（LLM判定が要る要素）173個は未判定のまま（全要素の17%）
- fixture に無い型の捏造は検出できない
- 他の文書での誤検出率は不明（防衛白書166問での実測のみ）

### 次: ステップ2 — 出荷中の捏造 Q158 を潰す

「2025年度の概算要求額」を問われ、白書に記載が無いのに当初予算の数値を答える。
**単発ではなく類型の可能性**があり、修理した判定器なら同種を機械的に数えられる。

## 2026-08-05 22:05 — クッション文数24を撤回 — 捏造が1→3件に増えていた

`main` @ `4025144` / 未コミットの変更あり

### 結論: **この変更は出荷しない。テンプレートから撤回済み。**

`SENTENCE_CUSHION_TOPK` を 8→24 にすると要素カバー率は上がるが、
**真の捏造が 1件 → 3件に増える**。顧客要件は「出典必須・文書外は不明」であり、
カバー率の改善では相殺できない。

| | TOPK=8 | TOPK=24 |
|---|---|---|
| 166問 要素カバー率 | 0.646 | 0.675 (+2.9pt) |
| C5 推論・含意 | 0.465 | 0.581 (+11.6pt) |
| C4 要約 | 0.570 | 0.653 (+8.3pt) |
| **真の捏造** | **1件** | **3件** |

新規の捏造:
- Q139 (C4): 「2021年に策定された防衛省気候変動対処戦略」← 策定年の誤り
- Q17 (C5): 「2030年度以降、次期防衛通信衛星の運用開始により…」← 文書に無い断定
- Q158 (C6): 概算要求額を「8兆4,748億円」と断定（TOPK=8 の 77,249億円から別の誤値に）

### 🔴 私が犯した誤り3つ（自戒）

**1. 壊れた指標で安全性を判定した。**
`[X]` 発火が 11→8 に減ったのを見て「捏造の増加なし」と報告した。しかし
消えた6件は自分で「誤検出」と裁定済みのもの。**実質は増えていた。**
「[X] は誤検出91%で信用できない」と自分で結論しておきながら、
その指標で安全性を判断した。

**2. 既知の実験を「新発見」として報告した。**
`docs/TOPN_CUSHION_EXPERIMENT_2026-07-26.md` に同じ変数の検証が記録済みで、
**非単調性の解消も、クッションを緩めると捏造が増えること（0/60→3/60）も
実測されていた**。同文書の推奨は「当面 既定8 のまま」。
着手前に既存文書を探さなかった。

**3. 測っていない構成に測定値を書き込んだ。**
Linux テンプレートは gemma4:12b + bge-m3 だが、測定は granite4.1:8b +
granite-embedding。「同一構成での実測」と書いたのは誤り。

### 有効だった知見（捨てないもの）

- 非単調性の機序（クッションが上位N文で固定 → チャンク増で押し出される）は正しい。
  ただし**7月に既知**であり、私の発見ではない
- 弱点の所在（C4 要約・C5 推論の取りこぼし）は `[N]/[E]` 判定に基づき有効
- 採点器の棄権判定の欠陥3件の修正（コミット bf08a47）は有効
- **[X] 判定は誤検出91%で使い物にならない**。judge 移行が必要

### 次にやること

1. `Cannonball results` ログを収集し、**プロンプト切り詰めで捏造禁止ルールが
   消えていないか**を確認する（7月の文書が §3-3 で設計欠陥として記録している）
2. 実利を取るなら 12 か 16 を、**捏造の全数精査つき**で測る。
   24 は topN=8 では本文3,898字＝上限飽和で、実質クッションOFF に近い
3. `[X]` の judge 移行（これが無いと安全性を測れない）

## 2026-08-05 13:37 — v1.2.9 公開 — 全モデル非中国系を達成

`main` @ `a7086d8` / 未コミットの変更あり

### 結果: **公開完了**

```
https://github.com/ishihara1447/localRAG/releases/tag/v1.2.9
OTE-RAG-win64-v1.2.9.zip  7,918,921,939 bytes
  SHA-256 ea17f58561379ffd300a2b46d01eb632f6b3065f3ed8e329a49e78de6c00ef3a
```

公開後に実際にダウンロードし、`.sha256` と `MANIFEST.txt` がビルド時のハッシュと
一致すること、README の手順順序が修正済みであることを確認した。

### ゴール達成: 同梱する全AIモデルが非中国系になった

| 種別 | モデル | 出自 |
|---|---|---|
| 生成AI | granite4.1:8b | IBM（米）/ Apache-2.0 |
| **文書検索** | **granite-embedding:278m** | **IBM（米）/ Apache-2.0** |
| 並べ替え | japanese-reranker-xsmall-v2 | 日本 / MIT |

`bge-m3` の重みは配布物に1バイトも残っていない。

### 測定（事前登録 → 測定 → 判定）

対照 `bge-m3` を**同一日・同一手順で測り直した**うえでの比較。

| | 検索 @8 | 生成30問 | 応答中央値 |
|---|---|---|---|
| 対照 bge-m3 | 0.7120 | 28/30 | 4.9s |
| **採用 granite-embedding** | **0.7284（+1.64pt）** | 27/30（−1） | 5.0s |
| 候補 embeddinggemma | 0.7164 | 28/30 | **17.7s** ⚠️ |

3条件とも3run が1問単位で完全一致（ばらつき0）。
**embeddinggemma は応答が3.6倍遅く**、TTFT 8秒要件に直結するため不採用
（判定基準に速度を入れていなかったのは事前登録の不備。後から基準を足したのではなく
「基準外だが採否を左右する事実」として記録した）。

**事前予想は2つとも外れた**（「候補Aは日本語で不利」「どちらも P1 を満たさない可能性が
相応にある」→ 実際は両候補とも合格し、候補Aは検索で対照を上回った）。

### 🔴 測定を無効にしかけた罠（再掲・重要）

最初の再埋め込みが**2秒で完了**し、ログに `Using cached data to save on embed costs`。
**前条件のベクトルが再利用され、候補モデルが一度も動いていなかった。**
そのまま測れば「候補が対照と完全に同一」という無意味な結果が出て、
しかも成功したように見えた。

→ `out/embswap/run-condition.sh` にキャッシュ再利用の検知とエラー停止を組み込んだ。
**埋め込みを変える測定では `runtime/anythingllm-storage/vector-cache/` を必ず消すこと。**

### 公開前レビューが止めた不具合（顧客に実害が出るもの）

1. 🔴 **同梱 `SECURITY_GUIDE.md` が「bge-m3 / bge-reranker-v2-m3 を同梱」と記載**。
   どちらも BAAI（中国）で、Release 本文の主張を現物が否定していた。
   `docs/customer-windows/` が v1.2.0 時代のまま放置されていたのが原因
2. 🔴 **README.txt の手順で分割ファイルの削除が Setup の後にあり、容量不足で失敗する**。
   残したまま = 44.0 GiB 必要（記載は40GB）。しかも約8GBの照合と約10GBの展開を
   終えた後に停止するため、10分以上待たされてからの失敗
3. 開発者向け作図指示書と旧構成の SVG が顧客に届いていた（v1.2.8 と同型の再発）
   → `export-windows.ps1` の robocopy から除外
4. 製品本体サイズ「約9.4GB」は机上の引き算で誤り。実測 約9.9GB
5. インストール先を C: 以外にしても **C: に約22GB 必要**（一時展開とモデル置き場が C: 固定）

### 前提工事

Ollama 埋め込みエンジンに接頭辞対応を実装（fork `741f654`）。
未実装のまま測ると接頭辞前提の候補が不当に不利になる。変異注入で検出力を確認済み。

### 残件

- **VRAM 8GB 実機での動作は未検証**（実機が無い）
- **v1.2.9 でのインストール通し実行の記録が無い**（ビルドと結合は検証済み）
- **Linux 版 v1.1.2** はビルド済み・未公開のまま。LLM が gemma4:12b、
  埋め込みが bge-m3 で **Windows 版と構成が乖離**している。
  同じ差し替え（granite4.1:8b + granite-embedding）を当てて作り直すのが筋
- `docs/customer-windows/onepagers/*.svg` は旧構成のまま。作り直すまで同梱しない設定にした

## 2026-08-05 02:50 — Windows v1.2.8 公開待ち — レビュー指摘6件を修正して再ビルド

`main` @ `2823dc5`

### 状態: **公開のユーザー承認待ちで停止中**

```
/mnt/c/LocalRAG/dist/parts/
  OTE-RAG-Setup.exe
  OTE-RAG-win64-v1.2.8.zip.001 〜 .005
  OTE-RAG-win64-v1.2.8.zip.sha256   ← Setup.exe が必須とする
  Join-OTE-RAG.cmd / MANIFEST.txt / README.txt

ZIP  8,540,134,142 bytes
  SHA-256 c415b1d6d678ea8485904d08437f9032e7740ff804680eb2461b8faeabd996ba
```

公開コマンドは `gh release create v1.2.8 --notes-file windows-native/release-body-v1.2.8.md`。

### 公開前レビューが検出した6件（すべて対処済み）

1. 🔴 **`Join-OTE-RAG.cmd` が動かず、失敗時にダウンロードフォルダ全体の削除を促していた。**
   Windows のワイルドカードに文字クラス（`[0-9]`）は無い／`skip=1` がハッシュ行を捨てる／
   LF・UTF-8 で `rem` 解析が崩れる／`ZIPNAME` が空になり `del <folder>\*` に化ける。
   **4つとも独立に致命的。** 顧客と同じ CP932 コンソールで実機再現し、修正後に結合成功を確認。
2. 🔴 **`.sha256` が分割フォルダに入らず、`Setup.exe` が起動直後に例外終了する。**
   `PackageLocator` が必須としている。全顧客が確実に詰まる状態だった。
3. 🔴 **ディスク容量「約28GB」では `install.ps1` が停止する。** 実測で約38GB 必要（結合zip 7.9
   + 一時展開 9.8 + インストール先 9.8 が同時に存在し、その状態で 20GiB の空きを要求）。
4. 🔴 **精度表のラベルが条件違い（同型の誤り5回目）。**
5. 🔴 **NOTICE / THIRD_PARTY_NOTICES に granite の記載が無く、同梱していない gemma4 が載っていた。**
   ライセンス表示の欠落。**配布物を作り直して反映**（文書だけ直すと 10万件のチェックサム照合で停止する）。
6. 開発者向け README が顧客に届いていた → 顧客向け README.txt を生成する方式に変更。

### 🔴 精度表の誤りについて（5回目・自戒）

「v1.2.7 対 v1.2.8」と書いた表は、実際には**リランカーを両条件とも新モデルに固定して
LLM だけ入れ替えた**測定だった。左列は出荷されたことのない構成。
さらに出荷版 v1.2.7 の定義説明は **5/6** なのに **6** と書いた。
**これは本 WORKLOG に自分で「撤回済み・再び引用しないこと」と書いた数値そのもの。**
撤回リストを作りながら、そこから引いてしまった。

→ 本文に「出荷版どうしを同一条件で比較した測定は存在しない」と明記した。

### ゴール達成状況

| 要件 | |
|---|---|
| Windows 向けオフラインインストーラー | ✅ |
| 精度維持 | ✅ 27/30 |
| TTFT 8秒以内 | ✅ 24/24（中央値2.00s・最大2.65s） |
| VRAM 8GB | ⚠️ 7.3GB で載る見込み・**実機未検証**。`install.ps1` は 8,188MiB で警告して続行することを実装から検証済み |
| 非中国系 | ⚠️ **埋め込み `bge-m3`（BAAI）のみ未達** |

### 埋め込き差し替えの調査結果（未着手）

- `granite-embedding:278m`（IBM, Apache-2.0, 562MB）と `embeddinggemma:300m`（621MB）が入手可能
- **前提が欠けている**: Ollama 埋め込みエンジンの prefix 実装が 0 箇所。
  ruri 系・embeddinggemma は query/document で接頭辞を変える前提のため、
  実装（約20行）が無いまま測ると候補が不当に不利になる
- 既存導入先は全文書の再取り込みが必要。**新規導入ならコストゼロ**なので、
  8GB 機への初回導入に合わせるのが最も安価

### Linux 版 v1.1.2

ビルド・検証・分割・結合実証まで完了、**未公開**（`dist-linux/parts/`）。
ゴールが Windows 版だったため優先度を下げた。**LLM は gemma4:12b のまま**で granite 未反映。

## 2026-08-05 00:51 — Windows v1.2.8 完成・TTFT 8秒要件を達成（公開待ち）

`main` @ `9439f24` / 未コミットの変更あり

### 成果物

```
OTE-RAG-win64-v1.2.8.zip  8,540,133,890 bytes
  SHA-256 f455946b732dd8b66319f32f7130bf13d73f23593ef3191f6d877ecd1ea1e105
OTE-RAG-Setup.exe         26,624 bytes
5分割・結合ラウンドトリップ実証済み
```

**公開はまだ（ユーザー承認待ち）。**

### ゴールに対する達成状況

| 要件 | |
|---|---|
| Windows 向けオフラインインストーラー | ✅ 達成 |
| 精度維持 | ✅ 27/30（gemma4 と同点） |
| **TTFT 8秒以内** | ✅ **24/24 達成**（中央値2.00s・最大2.65s） |
| VRAM 8GB | ⚠️ 7.3GB で載る見込み・**実機未検証** |
| 非中国系 | ⚠️ 埋め込み `bge-m3`（BAAI）のみ未達 |

### TTFT の実測（16GB機）

| | 旧 | 新 |
|---|---:|---:|
| 中央値 | 6.99s | **2.00s** |
| p90 | 11.56s | **2.37s** |
| 最大 | 14.26s | **2.65s** |
| 8秒以内 | 14/24 | **24/24** |

**旧構成は24問中10問が要件を外していた。** 最大値が2.65秒に収まった点が重要。

🔴 **この比較は同一セッションの A/B ではない**（旧は 2026-08-04 測定）。
また LLM とリランカーを同時に変えているため改善の内訳は切り分けられない。
8GB 機での速度も未検証。

### 解決した詰まり（次回のため）

1. **`build-deps` が消失** → 稼働製品 `C:\LocalRAGProd` から回収できた
2. **`vite` が動かない** → `node_modules/.bin` のリンクが WSL 経由のコピーで失われる。
   `yarn install` をやり直せば復旧
3. **`frontend/index.html` が fork に無い** → 復元時に失われていた。今回は frontend の
   変更が無くビルド済み `server/public` を使用。**次に UI を変えるときは復元が必要**
4. コンテナの ollama は外部ネットワークを持たない（オフライン製品の設計）ため
   `ollama pull` ができない。使い捨てコンテナを通常ネットワークで起動して取得する
5. Windows の `ollama.exe` を WSL から叩くと Linux 側サーバに繋がる。
   モデルは blob を直接コピーするのが確実

### Linux 版 v1.1.2 の扱い

ビルド・検証まで完了しているが**未公開**（draft を削除済み、資材はディスクに残存）。
ゴールが Windows 版だったため優先度を下げた。公開する場合は
`dist-linux/parts/` と `linux-native/release-body-v1.1.2.md` がそのまま使える。
ただし**Linux 版は LLM が gemma4:12b のまま**で、granite への差し替えは未反映。

## 2026-08-04 22:37 — LLM を granite4.1:8b へ差し替え決定（VRAM 8GB 対応）

`main` @ `d2b445b` / 未コミットの変更あり

**ゴール**: Windows / VRAM 8GB で動く製品とオフラインインストーラーを作り、Release 公開する。

### 決定

LLM を `gemma4:12b` → **`granite4.1:8b`（IBM, Apache-2.0）** に差し替える。

| | gemma4:12b | granite4.1:8b |
|---|---|---|
| 精度（防衛白書30問・各3run） | 27/30 | **27/30**（同点） |
| VRAM（ctx 8192） | 8.4 GB | **6.7 GB** |
| + bge-m3 | 9.0 GB ← **8GBに載らない** | 7.3 GB |

**理由**: 精度が同点で VRAM が 1.7GB 減る。8GB 要件に対して gemma4 は明確に超過。

### 🔴 未確定（採否を最終決定できていない）

**VRAM 6.7GB は事前登録の基準 P5（6.5GB以下）に未達。** 8GB の実効容量
7.5〜7.8GiB に対し合計 7.3GB で余裕が無い。**導入先 8GB 機の `nvidia-smi` が
取れれば確定する**（クリティカルパス）。基準を緩めたのではなく判定を保留している。

代替: ctx 4096 なら 5.9GB（合計 6.5GB）に収まるが、精度の代償は未測定。

### 記録すべき観察

- **合計は同点でも中身が違う**: 直接事実 +1、数値判別 +1、定義説明 **−2**
- **granite の失点2問は知識の誤りではなく採点器のキーワード不一致**。
  「持たず/作らず/持ち込ませず」を「持たない/製造をしない/導入を受け入れない」と
  言い換えて全滅。**スコアは動かさない**（採点器は凍結した測定器のため）が、
  定型句を再現しない傾向は実務で不利に働きうる
- granite の回答は gemma4 の約2.5倍長い（270字 対 106字）

### 事前予想は外れた

「2〜4問落ちる」→ 実際は ±0。「VRAM 5.9〜6.1GB」→ 実際は 6.7GB（ctx 8192 を見落としていた）。

### Windows 版で先に直した不具合

`windows-native/config/server.env.template` に `RERANKER_MODEL_PREF` が無く、
コード既定が旧リランカー（BAAI・中国系）を指していた。**このまま再ビルドすると
モデルは同梱されるのにサーバが見つけられず、エラーも出ないまま精度だけ落ちる**
状態だった。`OLLAMA_DISABLE_THINKING` / `QUERY_REFORMULATION` の欠落も併せて修正。

### 次

Windows インストーラーのビルド（build-deps 再構成 → export-windows.ps1 → Release）。
WSL から `powershell.exe` 経由でビルド可能（v1.2.6 に前例あり）。

## 2026-08-04 19:32 — リランカーを非中国系へ差し替え、v1.1.2 をビルド（公開直前）

`main` @ `7cd0099` / 未コミットの変更あり

### 何をしたか

顧客要件「モデル・リランカーに中国系を使わない」に対応し、リランカーを
`BAAI/bge-reranker-v2-m3` → `hotchpotch/japanese-reranker-xsmall-v2`（MIT）へ差し替え、
配布物 v1.1.2 をビルドした。**Release 公開はまだ**（ユーザー承認待ち）。

- 精度: 同一条件A/B（各3run・交互実行）で 27/30 対 27/30、Δ=±0
  - 内訳では増減あり: 定義説明 5/6→6/6、直接事実 7/8→6/8、他3カテゴリ不変
  - **旧と新で答えが分かれたのは30問中2問だけ**。この規模では差を検出する力がほとんど無い
- リランク所要: 中央値 約4,400ms → 約500ms（**実測で約1/9**）
- 同梱サイズ: 571MB → 37MB

### 🔴 撤回した数値（再び引用しないこと）

| 撤回したもの | なぜ誤りか |
|---|---|
| 「防衛白書 26/30 → 27/30 に改善」 | 26/30 は sessionId 導入前（2026-07-18）の別条件の記録 |
| 「28/30 vs 27/30 で −1」 | 28/30 は別ワークスペース・別時点 |
| 「精度・速度・サイズのすべてで上回った」 | 同梱データからは導けない。精度は同点 |
| 「定義説明 6/6 を維持」 | 実際は 5/6→6/6 の改善。かつ直接事実 7/8→6/8 の低下が併存 |
| 「計算量が約1/38」 | 層数×隠れ次元²の理論値。実測は約1/9 |
| 「95%CI −2.77〜+2.77」 | 不一致2件からの正規近似で、2桁の精度は根拠より精密 |

### 🔴 測定構成 ≠ 配布構成（未解決）

A/B 測定はイメージ `1.0.7` で行ったが、配布物は `1.1.0`。
実測で確認: `LANCE_HYBRID_RERANK` の出現数が 1.0.7 で **0**、1.1.0 で **1**。
配布構成ではリランカーの呼び出し回数が増えるため、**上記の精度・速度は
配布構成の値ではない**。配布構成での測定は未実施。

### 🔴 AVX2 依存が新規に入った（未検証）

同梱 ONNX は `model_qint8_avx2.onnx` をリネームしたもの。旧 bge は汎用
dynamic int8 で命令セット依存が無かったため、**本製品で初めて入ったハード依存**。
非対応環境では**エラーにならず静かに劣化する**可能性がある（未検証）。
`survey-target.sh` に検査項目5を追加して導入前に判明するようにした。

上流の変種は avx2 / avx512 / avx512_vnni / arm64 のみ。**AVX2 非対応の x86 CPU に
対する差し替え先は存在しない**ため、「別版へ差し替え可能」とは書けない。

### 失敗と原因（同じ轍を踏まないために）

1. **配布物を3回作り直した。** 文書を修正してパッケージへ反映する際、
   `checksums/package.sha256` を再生成しなかったため `install.sh` が整合性チェックで
   停止する状態になった。顧客はオフライン環境で12GBを再取得しても直らない。
   → **文書を1文字でも変えたらチェックサム再生成が必須。** 手順を守るだけでなく、
   `verify-package.sh` に検査項目 G（手順書の版数一致）を追加して機械的に捕まえるようにした。
2. **手順書が2版前のサイズを載せていた**（13,233,889,825）。v1.1.1 ですらない値が残留。
3. **`--version` がイメージタグまで動かしていた**ため、版数を上げるとビルドが停止した。
   `VERSION`（パッケージ版数）と `IMAGE_VERSION`（イメージタグ）を分離。
4. **`pkill -f '<文字列>'` が自分自身にマッチ**して launcher を殺した（複数回）。
   → プロセス確認は `ps -eo args | grep '[b]uild'` のブラケット記法か、成果物で判断する。
5. **待機ループの条件が早すぎた。** `[ -f MANIFEST.txt ]` は書き込み途中でも成立し、
   `[ -s ]` でも不十分だった（追記形式のため）。→ 完了マーカー（`join.sh` の生成＋
   全パートのハッシュ記録）で待つ。
6. **テストに検出力が無かった（2回）。** 実装を壊しても通るテストを書いていた。
   → 変異注入で FAIL することを必ず確認する。検査項目 G でも実施済み。

### 検証したこと

- `install.sh` の整合性チェックをそのまま実行 → 終了コード 0（44ファイル一致）
- `verify-package.sh` → 網羅性検証 PASS（検査項目 G を含む）
- **`join.sh` で結合を実証**（元ファイルを退避し、顧客と同じ手順で7分割から復元）
- 配布物に旧モデルの混入なし

### 残件

- Release `linux-v1.1.2` の公開（**承認待ち**）
- Windows 版は旧モデルのまま（v1.2.7）。`export-windows.ps1` は更新済みだが再ビルド未実施
- 埋め込み `bge-m3` は BAAI 製のまま（変更すると全文書の再取り込みが必要）
- 配布構成での精度・速度の測定
- AVX2 非対応環境での挙動
