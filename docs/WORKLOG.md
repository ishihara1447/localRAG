# WORKLOG — 作業の時系列記録（追記専用）

**このファイルは追記専用。過去のエントリは書き換えない。**
誤りが判明した場合も、古い記録を消さずに新しいエントリで訂正する
（「いつ何を信じていたか」が追えなくなると、同じ誤りを繰り返すため）。

## 読み方・書き方

| | `docs/HANDOFF.md` | `docs/WORKLOG.md`（本ファイル） |
|---|---|---|
| 形式 | 毎回**書き換える** | **追記のみ** |
| 内容 | 今どこにいるか・次に何をするか | 何を試し、何が失敗し、なぜそうしたか |
| 見るとき | セッション開始時にまず読む | 「なぜこうなっているのか」を遡るとき |

**セッション開始時は `HANDOFF.md` → 本ファイルの上から数エントリ、の順で読む。**

### 追記のしかた

```bash
scripts/worklog.sh "見出し" <<'EOF'
本文（markdown）
EOF
```

日時・ブランチ・git HEAD・未コミットの有無は自動で入る。

### 何を書くか

- **失敗と、その原因**（最重要。成功だけ書くと同じ失敗を繰り返す）
- 判断とその根拠（特に「やらないと決めたこと」とその理由）
- 撤回した数値・主張（撤回したことを明記する。後から誰かが再び引用するため）
- 外部に出したもの（Release 公開など、取り消せない操作）

### 何を書かなくてよいか

- コードの構造やコミット履歴から読み取れること
- 途中経過の細かい試行錯誤（結論と原因があればよい）

---

<!-- 新しい記録はこの行のすぐ下に入る -->

## 2026-08-05 00:51 — Windows v1.2.8 完成・TTFT 8秒要件を達成（公開待ち）

`main` @ `9439f24` / 未コミットの変更あり

### 成果物

```
OTE-RAG-win64-v1.2.8.zip  8,540,133,890 bytes
  SHA-256 f455946b732dd8b66319f32f7130bf13d73f23593ef3191f6d877ecd1ea1e105
OTE-RAG-Setup.exe         26,624 bytes
5分割・結合ラウンドトリップ実証済み
```

**公開はまだ（ユーザー承認待ち）。**

### ゴールに対する達成状況

| 要件 | |
|---|---|
| Windows 向けオフラインインストーラー | ✅ 達成 |
| 精度維持 | ✅ 27/30（gemma4 と同点） |
| **TTFT 8秒以内** | ✅ **24/24 達成**（中央値2.00s・最大2.65s） |
| VRAM 8GB | ⚠️ 7.3GB で載る見込み・**実機未検証** |
| 非中国系 | ⚠️ 埋め込み `bge-m3`（BAAI）のみ未達 |

### TTFT の実測（16GB機）

| | 旧 | 新 |
|---|---:|---:|
| 中央値 | 6.99s | **2.00s** |
| p90 | 11.56s | **2.37s** |
| 最大 | 14.26s | **2.65s** |
| 8秒以内 | 14/24 | **24/24** |

**旧構成は24問中10問が要件を外していた。** 最大値が2.65秒に収まった点が重要。

🔴 **この比較は同一セッションの A/B ではない**（旧は 2026-08-04 測定）。
また LLM とリランカーを同時に変えているため改善の内訳は切り分けられない。
8GB 機での速度も未検証。

### 解決した詰まり（次回のため）

1. **`build-deps` が消失** → 稼働製品 `C:\LocalRAGProd` から回収できた
2. **`vite` が動かない** → `node_modules/.bin` のリンクが WSL 経由のコピーで失われる。
   `yarn install` をやり直せば復旧
3. **`frontend/index.html` が fork に無い** → 復元時に失われていた。今回は frontend の
   変更が無くビルド済み `server/public` を使用。**次に UI を変えるときは復元が必要**
4. コンテナの ollama は外部ネットワークを持たない（オフライン製品の設計）ため
   `ollama pull` ができない。使い捨てコンテナを通常ネットワークで起動して取得する
5. Windows の `ollama.exe` を WSL から叩くと Linux 側サーバに繋がる。
   モデルは blob を直接コピーするのが確実

### Linux 版 v1.1.2 の扱い

ビルド・検証まで完了しているが**未公開**（draft を削除済み、資材はディスクに残存）。
ゴールが Windows 版だったため優先度を下げた。公開する場合は
`dist-linux/parts/` と `linux-native/release-body-v1.1.2.md` がそのまま使える。
ただし**Linux 版は LLM が gemma4:12b のまま**で、granite への差し替えは未反映。

## 2026-08-04 22:37 — LLM を granite4.1:8b へ差し替え決定（VRAM 8GB 対応）

`main` @ `d2b445b` / 未コミットの変更あり

**ゴール**: Windows / VRAM 8GB で動く製品とオフラインインストーラーを作り、Release 公開する。

### 決定

LLM を `gemma4:12b` → **`granite4.1:8b`（IBM, Apache-2.0）** に差し替える。

| | gemma4:12b | granite4.1:8b |
|---|---|---|
| 精度（防衛白書30問・各3run） | 27/30 | **27/30**（同点） |
| VRAM（ctx 8192） | 8.4 GB | **6.7 GB** |
| + bge-m3 | 9.0 GB ← **8GBに載らない** | 7.3 GB |

**理由**: 精度が同点で VRAM が 1.7GB 減る。8GB 要件に対して gemma4 は明確に超過。

### 🔴 未確定（採否を最終決定できていない）

**VRAM 6.7GB は事前登録の基準 P5（6.5GB以下）に未達。** 8GB の実効容量
7.5〜7.8GiB に対し合計 7.3GB で余裕が無い。**導入先 8GB 機の `nvidia-smi` が
取れれば確定する**（クリティカルパス）。基準を緩めたのではなく判定を保留している。

代替: ctx 4096 なら 5.9GB（合計 6.5GB）に収まるが、精度の代償は未測定。

### 記録すべき観察

- **合計は同点でも中身が違う**: 直接事実 +1、数値判別 +1、定義説明 **−2**
- **granite の失点2問は知識の誤りではなく採点器のキーワード不一致**。
  「持たず/作らず/持ち込ませず」を「持たない/製造をしない/導入を受け入れない」と
  言い換えて全滅。**スコアは動かさない**（採点器は凍結した測定器のため）が、
  定型句を再現しない傾向は実務で不利に働きうる
- granite の回答は gemma4 の約2.5倍長い（270字 対 106字）

### 事前予想は外れた

「2〜4問落ちる」→ 実際は ±0。「VRAM 5.9〜6.1GB」→ 実際は 6.7GB（ctx 8192 を見落としていた）。

### Windows 版で先に直した不具合

`windows-native/config/server.env.template` に `RERANKER_MODEL_PREF` が無く、
コード既定が旧リランカー（BAAI・中国系）を指していた。**このまま再ビルドすると
モデルは同梱されるのにサーバが見つけられず、エラーも出ないまま精度だけ落ちる**
状態だった。`OLLAMA_DISABLE_THINKING` / `QUERY_REFORMULATION` の欠落も併せて修正。

### 次

Windows インストーラーのビルド（build-deps 再構成 → export-windows.ps1 → Release）。
WSL から `powershell.exe` 経由でビルド可能（v1.2.6 に前例あり）。

## 2026-08-04 19:32 — リランカーを非中国系へ差し替え、v1.1.2 をビルド（公開直前）

`main` @ `7cd0099` / 未コミットの変更あり

### 何をしたか

顧客要件「モデル・リランカーに中国系を使わない」に対応し、リランカーを
`BAAI/bge-reranker-v2-m3` → `hotchpotch/japanese-reranker-xsmall-v2`（MIT）へ差し替え、
配布物 v1.1.2 をビルドした。**Release 公開はまだ**（ユーザー承認待ち）。

- 精度: 同一条件A/B（各3run・交互実行）で 27/30 対 27/30、Δ=±0
  - 内訳では増減あり: 定義説明 5/6→6/6、直接事実 7/8→6/8、他3カテゴリ不変
  - **旧と新で答えが分かれたのは30問中2問だけ**。この規模では差を検出する力がほとんど無い
- リランク所要: 中央値 約4,400ms → 約500ms（**実測で約1/9**）
- 同梱サイズ: 571MB → 37MB

### 🔴 撤回した数値（再び引用しないこと）

| 撤回したもの | なぜ誤りか |
|---|---|
| 「防衛白書 26/30 → 27/30 に改善」 | 26/30 は sessionId 導入前（2026-07-18）の別条件の記録 |
| 「28/30 vs 27/30 で −1」 | 28/30 は別ワークスペース・別時点 |
| 「精度・速度・サイズのすべてで上回った」 | 同梱データからは導けない。精度は同点 |
| 「定義説明 6/6 を維持」 | 実際は 5/6→6/6 の改善。かつ直接事実 7/8→6/8 の低下が併存 |
| 「計算量が約1/38」 | 層数×隠れ次元²の理論値。実測は約1/9 |
| 「95%CI −2.77〜+2.77」 | 不一致2件からの正規近似で、2桁の精度は根拠より精密 |

### 🔴 測定構成 ≠ 配布構成（未解決）

A/B 測定はイメージ `1.0.7` で行ったが、配布物は `1.1.0`。
実測で確認: `LANCE_HYBRID_RERANK` の出現数が 1.0.7 で **0**、1.1.0 で **1**。
配布構成ではリランカーの呼び出し回数が増えるため、**上記の精度・速度は
配布構成の値ではない**。配布構成での測定は未実施。

### 🔴 AVX2 依存が新規に入った（未検証）

同梱 ONNX は `model_qint8_avx2.onnx` をリネームしたもの。旧 bge は汎用
dynamic int8 で命令セット依存が無かったため、**本製品で初めて入ったハード依存**。
非対応環境では**エラーにならず静かに劣化する**可能性がある（未検証）。
`survey-target.sh` に検査項目5を追加して導入前に判明するようにした。

上流の変種は avx2 / avx512 / avx512_vnni / arm64 のみ。**AVX2 非対応の x86 CPU に
対する差し替え先は存在しない**ため、「別版へ差し替え可能」とは書けない。

### 失敗と原因（同じ轍を踏まないために）

1. **配布物を3回作り直した。** 文書を修正してパッケージへ反映する際、
   `checksums/package.sha256` を再生成しなかったため `install.sh` が整合性チェックで
   停止する状態になった。顧客はオフライン環境で12GBを再取得しても直らない。
   → **文書を1文字でも変えたらチェックサム再生成が必須。** 手順を守るだけでなく、
   `verify-package.sh` に検査項目 G（手順書の版数一致）を追加して機械的に捕まえるようにした。
2. **手順書が2版前のサイズを載せていた**（13,233,889,825）。v1.1.1 ですらない値が残留。
3. **`--version` がイメージタグまで動かしていた**ため、版数を上げるとビルドが停止した。
   `VERSION`（パッケージ版数）と `IMAGE_VERSION`（イメージタグ）を分離。
4. **`pkill -f '<文字列>'` が自分自身にマッチ**して launcher を殺した（複数回）。
   → プロセス確認は `ps -eo args | grep '[b]uild'` のブラケット記法か、成果物で判断する。
5. **待機ループの条件が早すぎた。** `[ -f MANIFEST.txt ]` は書き込み途中でも成立し、
   `[ -s ]` でも不十分だった（追記形式のため）。→ 完了マーカー（`join.sh` の生成＋
   全パートのハッシュ記録）で待つ。
6. **テストに検出力が無かった（2回）。** 実装を壊しても通るテストを書いていた。
   → 変異注入で FAIL することを必ず確認する。検査項目 G でも実施済み。

### 検証したこと

- `install.sh` の整合性チェックをそのまま実行 → 終了コード 0（44ファイル一致）
- `verify-package.sh` → 網羅性検証 PASS（検査項目 G を含む）
- **`join.sh` で結合を実証**（元ファイルを退避し、顧客と同じ手順で7分割から復元）
- 配布物に旧モデルの混入なし

### 残件

- Release `linux-v1.1.2` の公開（**承認待ち**）
- Windows 版は旧モデルのまま（v1.2.7）。`export-windows.ps1` は更新済みだが再ビルド未実施
- 埋め込み `bge-m3` は BAAI 製のまま（変更すると全文書の再取り込みが必要）
- 配布構成での精度・速度の測定
- AVX2 非対応環境での挙動
