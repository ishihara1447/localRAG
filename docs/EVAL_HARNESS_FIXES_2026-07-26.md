# 評価ハーネスの欠陥3件の修正記録（2026-07-26）

対象: `docs/QUALITY_ROADMAP_2026-07-26.md` §2.5.6「M1 で発見された評価ハーネスの欠陥」の #1〜#3。
発見元: `docs/PHASE_A_IMPLEMENTATION_2026-07-26.md`（S07 の副次的発見・後始末の節）。

制約（ユーザー明示指示）:
- `anything-llm/` 配下は**一切変更しない**（他セッションの未コミット変更が大量にあるため）
- git commit / push はしない
- 変更は `scripts/` と `fixtures/` に限定
- ワークスペース設定（topN=8 / temp=0 / mode=query）を作業後に復元する
- **チャット履歴とワークスペースは削除しない**（不可逆であり、ユーザーの手動テストが混在しうる）
- 数値はすべて実行して得た実測値。推測値は書かない

検証環境:

| 項目 | 値 |
|---|---|
| AnythingLLM dev | `http://localhost:3001`（`/api/ping` → `{"online":true}`）、コンテナ `anythingllm` healthy |
| 白書ワークスペース slug | `5ec7ec63-b421-42dd-8610-54984f28abc2` |
| 投入文書 | `R07zenpen.pdf-4b0b11c3-….json`（544物理ページ） |
| WS設定 | `chatMode=query` / `openAiTemp=0` / `topN=8` / `similarityThreshold=0.25` / `openAiHistory=20` |

変更したファイル:

| ファイル | 変更 |
|---|---|
| `scripts/hakusho-eval.py` | 設問ごとに一意な `sessionId` を付与（タスク1） |
| `scripts/scale-eval.py` | 設問ごとに一意な `sessionId` を付与＋ワークスペース削除の戻り値検査（タスク1・2） |
| `scripts/ambiguous-eval.py` | **変更なし**（既に `sessionId` を分離済みであることを確認） |
| `fixtures/complex/hakusho-complex-qa.json` | Q19 に `gold_pages`（目次 phys 5〜8）を付与（タスク3） |

---

## タスク1: `hakusho-eval.py` に `sessionId` を追加し、ベースラインを取り直す

### 1-1. 修正内容

`scripts/hakusho-eval.py` の `ask()` に `sessionId` を追加した。
形式は complex-eval と同方式（`hakusho-<run>-<設問番号2桁>`）。
`run` は環境変数 `HAKUSHO_RUN`、未指定なら `uuid4().hex[:8]`。
実行ログJSONの各設問に `session` を記録する。**採点ロジックは一切変更していない。**

`scripts/ambiguous-eval.py` は**確認の結果、既に分離済み**だった
（`amb-eval-<run>-<設問>-<clear|amb>`。2026-07-18 の実装時から入っている）→ **変更なし**。
`scripts/scale-eval.py` は**入っていなかった**ので追加した（`scale-eval-<run>-<設問番号>`）。

### 1-2. なぜ交絡するのか（コードで確認した機序）

`anything-llm/server/utils/chats/index.js:61-81` の `recentChatHistory()` は

```js
api_session_id: apiSessionId || null,   // ← sessionId 未指定なら NULL セッションを引く
```

で履歴を引き、`apiChatHandler.js:237` の `messageLimit = workspace?.openAiHistory || 20` 件を
**プロンプトに載せる**。本ワークスペースは `openAiHistory = 20`。

つまり `sessionId` を送らないと、**直前20件の質疑（他設問の回答や過去の手動テスト）が毎回コンテキストに入る**。
30問を順に投げれば、i問目は「同runのi-1問目まで＋前runの後半」を見ることになり、
**履歴の中身がrunごとに違う** ＝ 条件が固定できない。
実測でも、実験開始時点で既定セッション（`api_session_id IS NULL`）には
白書ワークスペースだけで**120件**の過去チャットが積まれていた。

### 1-3. 仮説の検証（実測）

**実験計画**: 唯一の違いが `sessionId` の有無になるよう、
新版から**チャットリクエストの `sessionId` だけを落とした対照群**を生成し
（差分は3行のみ。うち実質1行が `sessionId` の削除）、**交互に**実行した
（A1→B1→A2→B2→A3→B3）。連続実行だと後半の群だけ既定セッションの履歴が長くなり、
群と時間が交絡するため。

| 群 | 内容 |
|---|---|
| **A群** | `sessionId` **あり** = `scripts/hakusho-eval.py`（新版） |
| **B群** | `sessionId` **なし** = 対照群（チャットの投げ方は現行HEADと等価） |

**結果（実測）**

| run | 合計 | a) 直接事実 | b) 数値判別 | c) 定義説明 | d) 白書外 | e) 言い換え |
|---|---|---|---|---|---|---|
| **A1** | **28/30** | 7/8 | 6/6 | 5/6 | 5/5 | 5/5 |
| **A2** | **28/30** | 7/8 | 6/6 | 5/6 | 5/5 | 5/5 |
| **A3** | **28/30** | 7/8 | 6/6 | 5/6 | 5/5 | 5/5 |
| B1 | 29/30 | 8/8 | 6/6 | 6/6 | 5/5 | 4/5 |
| B2 | 28/30 | 8/8 | 6/6 | 5/6 | 5/5 | 4/5 |
| B3 | 29/30 | 8/8 | 6/6 | 6/6 | 5/5 | 4/5 |

**run間ばらつきの比較（これが仮説の検証）**

| 指標 | A群（sessionIdあり） | B群（sessionIdなし） |
|---|---|---|
| 合計スコア | 28 / 28 / 28 | 29 / 28 / 29 |
| **レンジ（max−min）** | **0** | **1** |
| 30問のOK/NGがrun間で一致 | **True** | **False**（Q20 が flip） |
| **回答本文が異なる設問数**（run対ごと） | **A1vsA2=0/30, A1vsA3=0/30, A2vsA3=0/30** | B1vsB2=**8/30**, B2vsB3=**8/30**, B1vsB3=0/30 |

> **A群は3run × 30問 = 90回答すべてがバイト単位で一致した。**
> これは complex-eval が6runバイト一致した既知の観測（Phase A S07）と整合する。

### 1-4. ★決め手: 検索結果は6run全てで同一だった

各設問の `sources`（ページ番号＋本文先頭60字）を全6run × 30問で突き合わせた結果、
**全runで完全一致**した（当然ではある。検索クエリは `message` だけで履歴を含まないため）。

```
A1: A1と検索結果が異なる設問=なし   回答本文が異なる設問数=0/30
A2: A1と検索結果が異なる設問=なし   回答本文が異なる設問数=0/30
A3: A1と検索結果が異なる設問=なし   回答本文が異なる設問数=0/30
B1: A1と検索結果が異なる設問=なし   回答本文が異なる設問数=23/30
B2: A1と検索結果が異なる設問=なし   回答本文が異なる設問数=24/30
B3: A1と検索結果が異なる設問=なし   回答本文が異なる設問数=23/30
```

**同じ検索結果・同じ temperature=0 から、履歴の有無だけで23〜24/30の設問の回答本文が変わる。**
かつ B群は群内でも揺れ、A群は一切揺れない。

**⇒ 仮説は実証された。run間のばらつきの原因は GPU 非決定性ではなく、チャット履歴の交絡である。**
（本環境の生成は、履歴を固定すれば決定的である。）

### 1-5. ★副作用: 履歴は「ノイズ」だけでなく「系統的なバイアス」でもあった

A群とB群で**判定が系統的に食い違う設問が2問**あった。どちらも**検索結果は同一**である。

| 設問 | A（履歴なし） | B（履歴あり） | 中身 |
|---|---|---|---|
| **Q07**（a・かがに着艦した機種） | **NG（3run全て）** | **OK（3run全て）** | A:「…記述はありません」と**拒否**。B:「F-35Bです」と正答 |
| **Q26**（e・新司令部の発足時期） | **OK（3run全て）** | **NG（3run全て）** | A:「2025年**3月24日**」。B:「2025年3月」で**日が欠落** |
| Q20（c・交戦権） | NG（3run全て） | **OK/NG/OK と揺れる** | 定義の後半（「交戦国が国際法上有する種々の権利の総称」）を書くか否かで揺れる |

**Q07 は特に重要**: 取得チャンクの **rank 1・2・4・5 に `F-35B` が入っている**（実測）にもかかわらず、
履歴が無いと拒否する。**これは retrieval の失敗ではなく生成側の過剰拒否**である。
旧ベースラインでは履歴が偶然この過剰拒否を打ち消していたため、**欠陥が見えていなかった**。

つまり「28〜29/30」という旧値は、**ノイズが乗っていただけでなく、
測定対象でない履歴の効果が混ざった数値**だった。

### 1-6. 新ベースライン（旧値と併記）

> **⚠ これはベースラインの定義変更である。旧ベースラインとの連続性は無い。
> 以後、旧値と比較するときは必ず新旧を併記すること。**

| | **新ベースライン（sessionId あり）** | 旧ベースライン（sessionId なし） |
|---|---|---|
| 定義 | 設問ごとに独立セッション。履歴なし | 全30問が既定セッションに蓄積。直前20件が履歴に入る |
| **合計** | **28/30（3run: 28 / 28 / 28、レンジ 0）** | 28〜29/30（平均28.5。今回の再現実測: 29/28/29） |
| a) 直接事実 | **7/8** | 8/8 |
| b) 数値判別 | **6/6** | 6/6 |
| c) 定義説明 | **5/6** | 5〜6/6 |
| d) 白書外(不明) | **5/5** | 5/5 |
| e) 言い換え | **5/5** | 4/5 |
| 決定性 | **決定的**（90回答バイト一致） | 非決定的（8/30 の回答が run 間で変わる） |
| 測定日 | 2026-07-26 | 2026-07-16〜07-26 |
| 条件 | `gemma4:12b` / think:false / hybrid+cushion / topN=8 / temp=0 / query / 白書1文書 | 同左 |

**新ベースラインで NG の2問**（＝今後の改善対象）:

| 設問 | カテゴリ | 失敗モード | 分類 |
|---|---|---|---|
| Q07 護衛艦「かが」に着艦した機種 | a | `F-35B` が rank1,2,4,5 に**取れているのに**「記述はありません」と拒否 | **(C) 生成失敗（過剰拒否）** |
| Q20 憲法9条2項の「交戦権」 | c | `交戦国` を含むチャンクが rank1 に**取れているのに**、定義の後半を書かず kw 3語のうち一部のみ | **(C) 生成失敗（回答の不完全）** |

**2問とも retrieval は成功しており、生成側の問題である。**
旧ベースラインの既知failure（「(b)個室化＝チャンク境界の語中分断」「(e)新司令部＝日が欠落」）とは
顔ぶれが変わっている点に注意（(b)は6/6で解消、(e)は履歴なしだと正答する）。

### 1-7. この検証の限界

- **3run である**。A群のレンジ0は「3runでたまたま一致した」可能性を完全には排除しない。
  ただしA群は**回答本文が90件バイト単位で一致**しており、単なるスコア一致より強い証拠である
- B群の履歴は**既定セッションに既に120件あった状態**から始まっている。
  履歴が空の状態から始めれば B群の挙動は変わりうる（＝そもそも再現不能というのが問題の本質）
- 新ベースラインは**この文書・このモデル構成に対する値**であり、他条件へは持ち越せない

---

## タスク2: `scale-eval.py` のワークスペース削除漏れ

### 2-1. 原因（コードを読んで確定。`anything-llm/` は変更していない）

- `runtime/docker-compose.yml:87` に **`WORKSPACE_DELETION_PROTECTION=1`**
- `anything-llm/server/utils/middleware/workspaceDeletionProtection.js` は
  この環境変数が**存在するだけで**（値によらず）**403** を返す
- `scale-eval.py` は `client.delete(...)` の**戻り値を見ていなかった**ため、403 が沈黙していた

実測（存在しない slug に対する安全なプローブ。ミドルウェアは slug 解決の前に応答する）:

```
status: 403
body  : {"success":false,"error":"Workspace deletion is blocked by the system administrator."}
```

### 2-2. ★重要な訂正: **UI からも削除できない**

Phase A の記録は「UIから手動削除する（APIでは消せない）」としていたが、これは**誤り**である。
`workspaceDeletionProtection` は**3つの削除経路すべて**に付いている:

| 経路 | ファイル:行 | 用途 |
|---|---|---|
| `DELETE /api/v1/workspace/:slug` | `endpoints/api/workspace/index.js:230` | 開発者API（スクリプトが使う） |
| `DELETE /workspace/:slug` | `endpoints/workspaces.js:279` | **UI の削除ボタン** |
| `DELETE /admin/workspaces/:id` | `endpoints/admin.js:300` | 管理者画面 |

したがって、この環境変数が有効な限り**どの経路でも削除できない**。

### 2-3. 回避手段の調査結果

**評価実行時だけ保護を回避する正当な手段は存在しない。** 根拠:
- ミドルウェアは**リクエスト単位のオプトアウトを持たない**（ヘッダ・クエリ・ロールによる例外なし）。
  判定は `"WORKSPACE_DELETION_PROTECTION" in process.env` の1行のみ
- 環境変数はサーバプロセス起動時に固定されるため、**コンテナを作り直さずに外すことはできない**

→ **結論: 手動での後始末が必要**。唯一の正当な手順は次のとおり（**本作業では実行していない**）:

1. `runtime/docker-compose.yml` の `WORKSPACE_DELETION_PROTECTION=1` を一時的にコメントアウト
2. `docker compose up -d`（コンテナ再作成）
3. UI から対象ワークスペースを削除
4. 環境変数を戻して再度 `docker compose up -d`

**削除は不可逆であり、この作業には `runtime/` の変更とコンテナ再作成を伴うため、
本セッションでは一覧の提示に留めた。実行はユーザー判断。**

### 2-4. 現在漏れているワークスペースの一覧（実測）

`GET /api/v1/workspaces` と `GET /api/v1/workspace/:slug` で全件確認した。

| slug | 表示名 | 文書数 | 作成日時(UTC) | 判定 |
|---|---|---:|---|---|
| `5ec7ec63-b421-42dd-8610-54984f28abc2` | マイワークスペース | 1（`R07zenpen.pdf`） | 2026-07-18T00:47:48Z | **正規。削除してはならない** |
| **`scale-eval`** | scale-eval | **10**（`reg-01`〜`reg-10`） | **2026-07-26T05:24:09Z** | **★漏れ（削除対象）** |

**漏れている件数: 1件**（`scale-eval` のみ）。

> **ロードマップ §2.5.6 の「過去の全実行で漏れ続けている」という記述は、実測では成立していなかった。**
> 残っているのは Phase A の S02 検証で作られた1件だけである。理由は
> `Workspace.new()`（`models/workspace.js:199-203`）が**同名 slug の衝突時に
> `scale-eval-<8桁乱数>` を採番する**ため、複数回漏れていれば別 slug で複数残るはずだが、
> 実際には1件しか存在しない ＝ **保護機能の導入以降 scale-eval を回したのは1回だけ**、と読める。
> ただし今後は実行のたびに1件ずつ増える構造である（＝今回の修正が必要な理由）。

付随して残っている実体:

| 実体 | パス | サイズ |
|---|---|---|
| ベクタテーブル | `runtime/anythingllm-storage/lancedb/scale-eval.lance` | 376K |
| BM25 sidecar | `runtime/anythingllm-storage/lancedb/__oterag_fts__v2_scale-eval.lance` | 3.7M |
| 文書JSON | `runtime/anythingllm-storage/documents/custom-documents/reg-0*.json`（10件） | 80K |
| チャット | 既定セッション（`api_session_id` = NULL）に30件 | — |

### 2-5. 修正内容

`scripts/scale-eval.py` に `delete_workspace()` を新設し、`main()` の直接 `client.delete(...)` を置き換えた。

- **HTTP 200 以外は失敗として扱う**（例外も捕捉する）
- 失敗時は `!!!!` 枠で、**HTTPコード・サーバ応答・原因・残留物・後始末手順**を出力する
- 最終集計の後にも「削除できずに残っている」旨を再掲する（長いログの末尾で見落とさないため）

**動作検証（実測）**: 存在しない slug に対して `delete_workspace()` を呼び、
実在ワークスペースに一切触れずに失敗経路を通した。

```
!! ワークスペースの削除に失敗しました (HTTP 403) slug=definitely-not-a-real-slug-probe-xyz
!!   応答: {"success":false,"error":"Workspace deletion is blocked by the system administrator."}
!!   原因: サーバに WORKSPACE_DELETION_PROTECTION が設定されており、
!!         DELETE /api/v1/workspace/:slug は常に403で拒否される
!!         （UI からの削除経路 DELETE /workspace/:slug も同じ保護下）。
…
戻り値: False  （False ＝ 失敗を正しく検知している）
```

**`scale-eval.py` 本体は実行していない。** 実行すると規程10本入りのワークスペースが
**もう1件増える**（削除できないため）ためである。

---

## タスク3: Q19 に `gold_pages` を与える

### 3-1. 与えた gold と、その根拠

Q19「この白書の第Ⅵ部には何が書かれていますか」は unanswerable 設問である。
**正しく否定するには「本白書が第Ⅰ部〜第Ⅴ部の5部構成である」という事実を引けている必要がある**。
そこで目次ページを gold とした。

| phys | page（印字） | weight | 根拠 |
|---:|---|---:|---|
| **5** | null（目次1ページ目） | **2** | 「本文第Ⅰ部～第Ⅴ部の記述について」という**5部構成を直接示す唯一の記述**。実測（`PageIndex.count`）で全544ページ中 **1箇所のみ**（`第Ⅰ部～第Ⅴ部` の出現回数 = 1、出現ページ = phys 5） |
| 6 | null | 1 | 目次の続き（補助） |
| 7 | null | 1 | 目次の続き（補助） |
| 8 | null | 1 | 目次の続き（補助） |

物理5〜8が目次であることは実測で確認済み（`PHASE_A_IMPLEMENTATION_2026-07-26.md` §0-1 と同じ `PageIndex` で本文を目視）。
weight は **5部構成を直接立証できる phys 5 だけを必須(2)、残りの目次3ページを補助(1)** とした。
同じ目次を gold に持つ Q01（各部のタイトルを列挙させる設問）は4ページとも weight 2 だが、
Q19 が必要とするのは「5部しかない」という一点であり、それは phys 5 の1行で立証できるためである。

`gold_quotes` に「本文第Ⅰ部～第Ⅴ部の記述について」を追加した（正規化後の全文に**完全一致**することを検証済み。
S04 で既知の WARN 7件は増えていない）。**`anchors` は空のまま**にした。理由:
Q19 は「本文中の特定の語句を引けたか」ではなく「構造情報を引けたか」を問う設問であり、
アンカー方式の集計母数（n=19問）を動かさないため。ページ方式のみ n=20 になる。

`absence_proof`（`第Ⅵ部`/`第6部`/`第VI部`/`第六部` = すべて0回）は再実測して変化なしを確認した。

### 3-2. S05 再実行の結果（実測）

Q19 変更の**前後で連続して2回**実行した（同一セッション・同一環境）。

**ページ方式の集計値（旧 n=19問 → 新 n=20問）**

| k | Coverage 旧→新 | All-Hit 旧→新 | Hit Rate 旧→新 | MRR 旧→新 | nDCG 旧→新 |
|---:|---|---|---|---|---|
| 4 | 0.638 → **0.631** | 0.421 → **0.400** | 0.947 → **0.950** | 0.785 → **0.796** | 0.589 → **0.594** |
| **8**（既定topN） | **0.798 → 0.783** | **0.579 → 0.550** | 1.000 → **1.000** | 0.912 → **0.917** | 0.687 → **0.685** |
| 16 | 0.842 → **0.849** | 0.632 → **0.650** | 1.000 → **1.000** | 0.816 → **0.825** | 0.699 → **0.696** |
| 32 | 0.823 → **0.832** | 0.684 → **0.700** | 1.000 → **1.000** | 0.860 → **0.867** | 0.718 → **0.718** |

**アンカー方式は完全に不変**（Q19 のアンカーが空のままなので母数 n=19 が変わらない）:
Coverage 0.532 / 0.581 / 0.675 / 0.657、All-Hit 0.263 / 0.368 / 0.474 / 0.474、
Hit Rate 0.842 / 0.789 / 0.842 / 0.789、MRR 0.746 / 0.719 / 0.739 / 0.686（k=4/8/16/32）。

**カテゴリ別 Coverage（ページ方式）** — 変わるのは C6 のみ（n=1 → n=2）:

| カテゴリ | @8 旧→新 | @32 旧→新 | n 旧→新 |
|---|---|---|---|
| C1 集約・構成 | 0.472 → 0.472 | 0.528 → 0.528 | 4 → 4 |
| C2 マルチホップ | 0.933 → 0.933 | 1.000 → 1.000 | 5 → 5 |
| C3 比較 | 0.867 → 0.867 | 0.733 → 0.733 | 3 → 3 |
| C4 要約 | 0.889 → 0.889 | 1.000 → 1.000 | 3 → 3 |
| C5 推論・含意 | 0.778 → 0.778 | 0.778 → 0.778 | 3 → 3 |
| **C6 unanswerable** | **1.000 → 0.750** | 1.000 → 1.000 | **1 → 2** |

**Q19 単独の実測値**

| k | returned | 取得ページ（物理） | 被覆した gold | Coverage | MRR | nDCG |
|---:|---:|---|---|---:|---:|---:|
| 4 | 4 | 4, 10, 10, 9 | 5, 8 | 0.500 | 1.000 | 0.682 |
| 8 | 8 | 4, 202, 10, 10, 42, 25, 9, 72 | 5, 8 | 0.500 | 1.000 | 0.655 |
| 16 | 12 | 7, 202, 4, 10, 10, 5, 42, 51, 9, 72, 132, 369 | 5, 6, 7, 8 | 1.000 | 1.000 | 0.646 |
| 32 | 14 | 7, 4, 202, 10, 5, 275, 497, 42, 6, 105, 51, 72, 132, 369 | 5, 6, 7, 8 | 1.000 | 1.000 | 0.720 |

判定は「★kを増やせば取れる＝ランキングの問題」に分類された（@8=0.50 → @32=1.00）。
その結果、投資判断の根拠となる件数は
**「kを増やせば取れる」3問 → 4問（Q01/Q05/Q14/**Q19**）**、
**「kを増やしても取れない（索引・チャンク分割）」5問（Q02/Q03/Q04/Q10/Q16）は不変**。

> **⚠ 読むときの注意**: @4/@8 で被覆できている gold 5・8 は、
> 出所ページが phys **4** と **9** のチャンクを `PAGE_TOLERANCE=1`（±1許容）で拾ったものである。
> 目次そのもの（phys 5・6・7）が取れるのは @16 以降。
> ±1 許容はチャンク内多数決のずれを吸収する既存ルール（設計書 §3-3）を全設問一律に適用したものだが、
> Q19 の @8 = 0.50 は「目次に隣接するページが取れた」寄りの数値であり、
> **目次チャンクそのものは既定 topN=8 では取れていない**。
> これは C1（集約・構成）の Coverage@8 = 0.472 が示す弱点と同根である。

### 3-3. 他設問への影響: **なし**（機械照合で確認）

変更前後の設問別JSONを全設問 × 全k(4/8/16/32) × 全指標
（coverage / anchor_coverage / hit / anchor_hit / mrr / anchor_mrr / ndcg / returned /
page_covered / anchor_covered / pages）で突き合わせた（`scratchpad/fix/cmp_retrieval.py`）:

```
=== Q19以外の設問で差分があったもの ===
  なし（Q19以外は全設問・全k・全指標で完全一致）

全設問・全kで vector-search の取得ページ列が同一: True
```

**集計ロジックの問題は無い**。集計値が動いたのは母数が19→20に増えたことによる算術的な帰結だけである
（例: Coverage@8 = 旧 15.16/19 = 0.798 → 新 (15.16+0.50)/20 = 0.783）。
なお副次的に、**同一クエリの `vector-search` の取得結果が2回の実行でページ列まで完全一致**しており、
retrieval フェーズの決定性が改めて裏づけられた。

---

## 未対応の発見: `precision-eval.py` が同じ欠陥を2つとも持っている

本タスクの対象外（指示された対象は hakusho / ambiguous / scale の3本）だが、
調査中に `scripts/precision-eval.py` が**同じ2つの欠陥をどちらも持つ**ことが分かった。

| 箇所 | 内容 |
|---|---|
| `precision-eval.py:136-141` | `ask()` に `sessionId` が無い（全問が既定セッションに積まれる） |
| `precision-eval.py:201` / `:216` | `client.delete(...)` の戻り値を見ていない。**1回の実行でワークスペースを2つ作る**（`precision-single-doc` / `precision-multi-doc`）ので、**実行のたびに2件漏れる** |

現時点で `precision-*` のワークスペースは存在しない ＝ 保護機能の導入以降まだ実行されていない。
**手を入れていない理由**: 指示されたスコープ外であり、かつ修正の実地検証には実行が必要で、
実行すると（削除できないため）ワークスペースが2件増えてしまうため。
**次に precision-eval を使う前に、scale-eval と同じ2箇所の修正を入れること。**

---

## 後始末（実測で確認）

| 項目 | 状態 |
|---|---|
| 発行したAPIキー | **全削除済み**（7件削除 → `/api/system/api-keys` が `{"apiKeys":[]}`、DB の `api_keys` も0件） |
| ワークスペース設定 | **既定のまま変化なし**: `topN=8` / `openAiTemp=0` / `chatMode=query`（実行後にAPIで再取得して確認。復元操作は不要だった） |
| ワークスペース | **削除していない**（`5ec7ec63-…` と `scale-eval` の2件のまま） |
| チャット履歴 | **削除していない**（総数540件） |
| `anything-llm/` 配下 | **一切変更なし**（読み取りのみ） |
| `runtime/` 配下 | **変更なし** |

## 巻き戻し

```bash
git checkout scripts/hakusho-eval.py scripts/scale-eval.py   # タスク1・2
# タスク3: fixtures/complex/hakusho-complex-qa.json の Q19 の gold_pages を [] に戻し、
#          gold_quotes から「本文第Ⅰ部～第Ⅴ部の記述について」を除く
```

（`fixtures/complex/` は未コミットの新規ディレクトリなので `git checkout` では戻らない。）

---

## まとめ

| # | 欠陥 | 状態 | 結論 |
|---|---|---|---|
| 1 | `hakusho-eval.py` に `sessionId` が無い | ✅修正・検証済 | **仮説は実証された**。検索結果が6run同一なのに、履歴の有無だけで23〜24/30の回答本文が変わり、B群は群内でも揺れ、A群は90回答バイト一致。**新ベースライン 28/30（決定的）**。旧値 28〜29/30 とは非連続 |
| 2 | `scale-eval.py` のワークスペース削除漏れ | ✅修正・検証済 | 漏れは **1件（`scale-eval`、規程10本）**。「全実行で漏れ続けている」は実測では成立せず。**UIからも削除できない**（保護は3経路すべてに適用）ので、`runtime/docker-compose.yml` の環境変数を一時的に外す以外に手段は無い。**削除は実行していない** |
| 3 | Q19 に `gold_pages` が無い | ✅修正・検証済 | 目次 phys 5〜8 を付与。ページ方式が n=19→20 になり **Coverage@8 0.798→0.783**。**他19問の指標は全k・全指標で完全一致**＝集計ロジックに問題なし |

**この3件の修正で、生成側の評価も「条件を固定すれば決定的」になった。**
Phase B 以降の A/B 判定において、`hakusho-eval` の1点差は
（少なくとも本環境では）ノイズではなく実差として読めるようになった
— ただし n=30 では1点差の統計的有意性は依然として無い（Phase A S07 の検出力の議論は有効）。

---

# 追加修正（2026-07-26 夜・Claude Code 本体が実施）

> 本節は、担当サブエージェントが `_eval_common.py` の実装後に停止したため、**Claude Code 本体が引き継いで完了させた**分の記録である。

## 修正4: 数値一致の桁境界チェック（`_eval_common.py` ＋ `complex-eval.py` 配線）

**バグ**: 数値の一致判定が正規化後の単純な部分一致だったため、**桁の境界を跨いで誤一致**していた（S08 judge較正中に発覚）。

- `70000円` が `約270000円` の内部に一致 → 誤ってYES
- `17100` が `約1710000円` の内部に一致 → 誤ってYES

**採点バグはこれで3例目**（過去2回は空白由来。内部監査 §1-1）。

### 実装

- `scripts/_eval_common.py`: 番兵方式で桁境界を判定する `normalize_marked()` / `find_bounded()` / `contains_marked()` を追加（サブエージェントが実装）
- **`scripts/complex-eval.py`: `judge_element()` が旧来の `normalize(a) in na` のままで未配線だった** → `normalize_marked()` を1回実行して alias ループで使い回す形に変更（本体が実施）

### 検証（実測）

**単体**: 既知バグ2件が正しくNG、かつ退行なし（6/6合格）

| needle | haystack | 期待 | 実際 |
|---|---|---|---|
| `70000円` | `…約270000円です` | False | ✅ False |
| `17100` | `…約1710000円` | False | ✅ False |
| `70000円` | `…70000円です` | True | ✅ True |
| `84,748億円` | `…8兆4,748億円です` | True | ✅ True |
| `2025年3月24日` | `令和7年3月24日に発足` | True | ✅ True |

**受け入れ条件（本命）**: 保存済みのS06生成結果を再採点し、**自動採点が手動補正値に一致するか**。

```
決定的要素カバー率(修正後): 46/79 = 0.582
期待:                       46/79 = 0.582 （修正前 48/79 = 0.608）
→ 一致 ✅
判定が変わった要素: Q06('7万円'), Q11('17,100') の2件のみ
```

**既知の2件だけが反転し、他77要素は一切変化なし＝退行ゼロ。**

**既存3スクリプトへの影響**: `hakusho-eval` は本修正の適用後（`_eval_common.py` 変更は17:16、実験は以降）に実行され **28/30（新ベースラインと一致）**。退行なし。

## 修正5: `precision-eval.py` の欠陥3件

**このスクリプトは実行していない**（`WORKSPACE_DELETION_PROTECTION` により1回の実行でワークスペースが2件漏れるため）。検証は構文・import・単体関数で行った。

| # | 欠陥 | 対応 |
|---|---|---|
| 1 | `sessionId` を振っておらず設問が同一セッションに積まれる | `ask()` に `session` 引数を追加、`evaluate()` が `precision-<slug>-<n>` を渡す |
| 2 | ワークスペース削除の戻り値を検査せず、403を握りつぶす | `delete_workspace()` を新設（`scale-eval.py` と同方式）。403時は原因と後始末手順を表示 |
| 3 | **★新規発見**: `UNKNOWN_PATTERNS` に裸の `含まれて` `記載されて` が残存 | 否定形を要求する形に修正。**肯定文を「不明応答＝正解」と誤判定していた**（内部監査が指摘し hakusho-eval では修正済みだった既知バグの取り残し） |

### 検証（実測・5/5合格）

| 入力 | 期待 | 実際 |
|---|---|---|
| `この件は本マニュアルに定めがありません` | True | ✅ True |
| `出張日当は3,400円と記載されています` | **False** | ✅ False（旧実装は True＝誤判定） |
| `その数値は資料に含まれている` | **False** | ✅ False（旧実装は True＝誤判定） |
| `資本金の情報は文書に含まれていません` | True | ✅ True |
| `試用期間は3ヶ月です` | False | ✅ False |

### 所見

`precision-eval.py` は 2026-07-10 以降更新されておらず、現在のロードマップ上の評価体系（`complex-eval.py` 中心）には組み込まれていない。**今回の修正で欠陥は解消したが、次に使う際は「実行するとワークスペースが2件漏れる」制約が残っている**（サーバ側の保護設定を一時的に外す以外に回避手段がない）ことに注意すること。

## 意思決定ログ

| 日付 | 内容 | 担当 |
|---|---|---|
| 2026-07-26 | 桁境界チェックを `complex-eval.py` に配線し、保存済み回答の再採点で **46/79 = 0.582**（手動補正値と完全一致・他77要素無変化）を実測確認 | Claude Code 本体（担当エージェント停止のため引き継ぎ） |
| 2026-07-26 | `precision-eval.py` の欠陥3件を修正。うち1件（`UNKNOWN_PATTERNS` の裸パターン）は**本作業中に新規発見**した取り残し。**スクリプト自体は実行していない**（実行するとワークスペースが2件漏れるため） | Claude Code 本体 |

---

# 独立再検証（2026-07-27・サブエージェント）

> **経緯**: 修正4・修正5 を担当するサブエージェントが再度起動されたが、着手時点で
> **両修正はすでにコミット済み（`d4b0d94` / `f5d856f`）**だった。
> そこで**コードは一切変更せず、記録された数値をゼロから追試**した。本節は追試の結果と、
> そこで**新たに見つかった残存リスク**（§R-5）である。**コード変更・commit・push はしていない。**

## R-1. `normalize()` の返り値が修正前後で変わっていないこと（大規模照合）

桁境界チェックは「空白の跡を番兵として残す」実装であり、`normalize()` はその番兵を落とすだけになった。
**返り値が1文字でも変われば既存の一致判定すべてに波及する**ので、旧実装を復元して突き合わせた。

| 照合対象 | 件数 | `normalize()` の差分 |
|---|---:|---:|
| complex-eval の保存済み回答＋`sources` 本文（`s06_gen_run1〜9`） | — | 0 |
| retrieval 観測JSON（`s05_retrieval_run1/2`）中の全 `text` | — | 0 |
| hakusho / ambiguous の保存済み回答（`hak-A1〜A3`, `s02_*`, `s01_after`） | — | 0 |
| `fixtures/` 配下の全JSON・全txtの文字列 | — | 0 |
| 投入済み文書JSON（白書 544ページ ＝ 842,231字 ほか計11件） | — | 0 |
| **合計** | **4,083テキスト / 1,805,310字** | **0** |
| ランダム生成文字列（数字・カンマ・小数点・兆億万・全角・和暦・空白を含む文字集合、seed=20260726） | **200,000件** | **0** |

**⇒ `normalize()` は不変。桁境界の判定は `find_bounded()` / `contains_marked()` だけに閉じている。**

## R-2. 保存済み9run すべてでの再採点（記録は run1 のみだった）

| run | topN | 旧（素の部分一致） | 新（桁境界つき） | 反転した要素 |
|---|---:|---|---|---|
| s06_gen_run1〜6 | 8 | 48/79 = **0.608** | 46/79 = **0.582** | Q06/e3, Q11/e2 |
| s06_gen_run7 | 4 | 43/79 = 0.544 | 41/79 = **0.519** | Q06/e3, Q11/e2 |
| s06_gen_run8 | 4 | 42/79 = 0.532 | 40/79 = **0.506** | Q06/e3, Q11/e2 |
| s06_gen_run9 | 4 | 42/79 = 0.532 | 40/79 = **0.506** | Q06/e3, Q11/e2 |

- run1 は `--phase judge` を実際に流し、既存 `s06_judged_run1.json` と**要素単位で突き合わせて差分2件**
  （`Q06/e3` evidence=`7万円`, `Q11/e2` evidence=`17,100` が True→False）**のみ**であることを確認した。
- **9run すべてで反転するのは同じ2要素**であり、**topN=8 と topN=4 に等しく効く**。
  したがって **S07 の A/B 判定（topN 8 vs 4 の差は誤差範囲）の結論は変わらない**——
  `JUDGE_MODEL_CALIBRATION_2026-07-26.md` §4-3 の予想が実測で裏づけられた。

## R-3. 既存3スクリプトの退行チェック（保存済み回答のオフライン再採点）

記録では「修正後に実行して 28/30 だった」という時系列による確認だったので、
**同じ回答に現行 `grade()` を当て直す**方式で取り直した。

| 保存データ | 再採点 | カテゴリ内訳 | 記録された `ok` との不一致 |
|---|---|---|---|
| `hak-A1` / `A2` / `A3`（新ベースライン・sessionIdあり） | **28/30** ×3 | **a7/8 b6/6 c5/6 d5/5 e5/5** | **なし** |
| `s02_hakusho` / `s01_after`（旧・sessionIdなし） | 28/30 | a8/8 b6/6 c5/6 d5/5 e4/5 | なし |
| `s02_amb`（ambiguous 15問 × clear/amb） | — | — | なし |

**⇒ hakusho の新ベースライン 28/30（a7/8 b6/6 c5/6 d5/5 e5/5）は維持。退行なし。**
（そもそも hakusho / ambiguous / scale は `strip_think` と `squash_ws` しか import しておらず、
本修正が触れた `normalize()` 系には依存していない。上記はそれを実データで裏づけたもの。）

## R-4. 既存3スクリプトに同種の桁境界バグが無いか

`hakusho-eval` / `ambiguous-eval` は正規表現とキーワード一致で採点しており、
`80\s*時間` が `180時間` に、`70\s*周年` が `170周年` に当たる**同じ型のバグ**が原理的にありうる。
保存済み回答すべてで「一致文字列が数字で始まり直前も数字／数字で終わり直後も数字」を走査した。

| 対象 | 走査量 | 桁境界を跨いだ一致 |
|---|---|---:|
| hakusho-eval | 5run × 30問 | **0件** |
| ambiguous-eval | 15問 × clear/amb | **0件** |

**⇒ 現在の設問セットでは顕在化していない。** ただし**構造としては残っている**ので、
設問を増やすときは数値パターンに `(?<!\d)…(?!\d)` を付ける習慣にすること。

## R-5. ★新たに見つかった残存リスク: **retrieval 側（`_covered_anchors`）は素の部分一致のまま**

桁境界チェックが入ったのは生成側の要素判定（`judge_element`）だけである。
**`complex-eval.py` の `_covered_anchors()` は `na in t` のまま**であり、同じバグが残っている。
白書全文（正規化後 778,976字）に対して全アンカー70件・全 gold_quote 47件を走査した結果:

| 設問 | アンカー | 境界OKな出現 | 全出現 | 中身 |
|---|---|---:|---:|---|
| Q11 | `17,100` | **1** | 3 | 正当な出現は `訓練招集手当:17100~26300円/日`。残り2件は `約1710000円`（＝今回直した偽陽性と同じ箇所）と図表の軸数値の連なり |
| Q12 | `25,245` | **0** | 1 | 白書の原文は「2兆5,245億円」。正規化で `2524500000000` に展開されるため、**`25245` は常に桁の途中にしか現れない** |
| Q12 | `24,491` | **0** | 1 | 同上 |
| Q12 | `18,742` | **0** | 1 | 同上 |
| Q12 | `17,262` | **0** | 1 | 同上 |

gold_quotes 47件は**違反ゼロ**（S04 の原典検証は健全）。

**実測での影響（vector-search を1回だけ読み取り実行して確認）**

- **Q11**: 取得チャンク rank1 は**桁境界違反でしか一致しない**、rank2 が正当な一致。
  記録された `anchor_ranks["17,100"]` は **1** だが、正しくは **2**。
  ただし他の2アンカーが rank1 で一致しているため、**報告済みの
  Coverage / Hit / MRR の値は変わらない（＝S05 の集計値への影響はゼロ）**。
- **Q12**: 取得チャンクは素の一致でも桁境界つきでも**1件も当たらない**。
  したがって記録の `anchor_coverage = 0.000` は**測定アーティファクトではなく本物の検索失敗**である。

**⇒ 打ち手（次に触る人への申し送り）**

1. `_covered_anchors()` にも `contains_marked()` を入れるのが筋である。
2. ただし**そのまま入れると Q12 のアンカー4件は永久に一致しなくなる**（原文が「2兆5,245億円」で、
   アンカー `25,245` は展開後の13桁の内部にしか存在しないため）。
   **先に Q12 のアンカーを `2兆5,245億` 等の展開後に一致する表記へ書き直すこと。**
   同じ理由で `Q07/e4` の alias `21件`（白書中の出現は `521件`・`121件` のみ）のような
   「原文に単独では存在しない短い数値表記」も点検対象になる。

## R-6. `precision-eval.py`（修正5）の再確認

| 確認項目 | 方法 | 結果 |
|---|---|---|
| `sessionId` がリクエストボディに載るか | コード読解（`ask()` が `body["sessionId"] = session`、`evaluate()` が `precision-<slug>-<n>` を渡す） | ✅ |
| 削除失敗を検知するか | **存在しない slug への安全なプローブ**（実在WSに触れない） | ✅ **HTTP 403 → 警告枠 → 戻り値 `False`** |
| 通しでの実行 | **していない**（実行するとワークスペースが2件漏れるため） | — |

所見（記録の追認と補足）:

- 使用状況: 参照は `docs/` と `.claude/agents/dev-manager.md` のみで、**現行の評価体系（`complex-eval.py` 中心）からは呼ばれていない**。
  ただし `docs/EVAL_PROTOCOL_2026-07-27.md` は本スクリプトを「不明応答判定を持つスクリプト」として列挙しており、**廃止扱いにはなっていない**。
- 残る注意点: `sessionId` は `precision-<slug>-<n>` で、run 識別子を含まない。
  今は保護機能でワークスペースが消せず slug が毎回変わるため実害は無いが、
  **将来 slug を固定して再実行できるようになったら run 識別子を足すこと**（hakusho / scale は `<run>` を含む）。

## R-7. 後始末（実測）

| 項目 | 状態 |
|---|---|
| 発行したAPIキー | 2本発行（読み取り専用プローブ用）→ **いずれも即削除**。DB 実測で残ったのは**本作業前から存在するキーだけ**（作成時刻 13:46Z / 13:56Z に対し、本作業のプローブ実行は 14:16Z 以降）。**他セッションのキーは削除していない**（id 指定で自分の分だけ消した） |
| ワークスペース | **2件のまま**（`5ec7ec63-…` と既知の漏れ `scale-eval`）。**新規作成・削除ともにしていない** |
| ワークスペース設定 | **不変**（DB 直読み: `topN=8` / `openAiTemp=0` / `chatMode=query` / `similarityThreshold=0.25`） |
| チャット履歴 | **触っていない**（chat API を1回も呼んでいない。実行したのは vector-search 2回のみ） |
| `anything-llm/` `runtime/` | **変更なし**（読み取りのみ） |
| リポジトリへの変更 | **本節の追記のみ**。`scripts/` は1バイトも変更していない |
