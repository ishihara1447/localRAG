# 日本語ハイブリッド検索 Linux検証結果

実施日: 2026-07-15  
対象: 稼働中 AnythingLLM コンテナ / 防衛白書 workspace `hakusho-eval-57319043`

## 結論

- BM25検索の核心2件: **retrieval 2/2 PASS**
- 防衛白書30問: 生スコア20/30。27問目は「約3万2,000円」という正答を評価正規表現が表記ゆれとして受理しなかったため、内容を手動補正すると **21/30**。baseline 21/30を維持。
- 文書外質問: **5/5、不明応答維持**
- RAG E2E: **PASS=11 FAIL=0**
- ハルシネーション: 文書外5問で0件

## 反映確認

稼働中コンテナへ反映された実装とホストforkのSHA-256は一致した。

- `server/utils/vectorDbProviders/lance/index.js`
- `server/utils/vectorDbProviders/lance/hybridSearch.js`
- `LANCE_HYBRID_SEARCH=true`
- `OLLAMA_MODEL_PREF=gemma4:12b`
- `WORKSPACE_DEFAULT_TOP_N=8`

LanceDB sidecarは最終的に本体と同じ1,748行で構築された。初期ログに残る`rows: 10`は修正前の実行履歴であり、最終修正版では`rows: 1748`を確認した。

## 核心2件の診断

### 非核三原則

BM25の検索結果1位に、次の正解チャンクが入った。

`非核三原則とは、核兵器を持たず、作らず、持ち込ませずという原則を指し...`

APIの出典にも正解チャンクが含まれたが、gemma4は「定義の記載なし」と回答した。検索失敗ではなくLLM抽出失敗である。

### 交戦権

BM25の検索結果1位に、次の正解チャンクが入った。

`憲法第9条第2項は、「国の交戦権は、これを認めない。」と規定しているが、ここでいう交戦権とは...`

APIの出典0番目に正解チャンクが含まれたが、gemma4は「記載なし」と回答した。こちらも検索失敗ではなくLLM抽出失敗である。

## 30問結果

| カテゴリ | 生スコア | 内容補正後 |
|---|---:|---:|
| (a) 直接事実 | 6/8 | 6/8 |
| (b) 紛らわしい数値・年度 | 4/6 | 4/6 |
| (c) 定義・説明 | 2/6 | 2/6 |
| (d) 白書に無い情報 | 5/5 | 5/5 |
| (e) 言い換え・同義語 | 3/5 | 4/5 |
| 合計 | 20/30 | **21/30** |

失敗の中心は、正解チャンクが出典に含まれているにもかかわらず、gemma4が別チャンクを採用する、または正解チャンクを「記載なし」と判定するLLM抽出問題だった。BM25導入による検索取りこぼしは解消できている。

## E2E結果

既存の `scripts/rag-e2e-test.sh` を実行した。

- 文書内質問
- 日本語PDF
- DOCX
- 文書外質問
- 外部LLM providerのAPI拒否
- Swagger docs無効

全項目 `PASS=11 FAIL=0`。一時APIキーは別処理で削除済み。

## 判定と残課題

BM25実装の検索要件は合格。30問はbaselineを維持し、未知質問5/5とE2E 11/11も維持したため、検索改修としては採用可能な結果である。

一方、30問の期待値23/30には届かなかった。残る6件程度の失敗は検索ではなく、gemma4の抽出・選択問題である。改善候補は、正解チャンクの順位をさらに上げるコンテキスト制御、回答生成プロンプトの局所調整、または回答後の検証だが、未知質問5/5を壊さない回帰評価が必須である。

## 追加発見: APIキー一覧エンドポイント

評価後の後処理確認で、`GET /api/system/api-keys` が認証なしでもAPIキー一覧とsecretを返すことを確認した。これはhybrid検索の変更によるものではないが、顧客配布前の重大なハードニング課題である。

- 今回作成したCodex一時キーは対象名を限定して削除済み
- 既存のHakusho系キーは削除・変更していない
- 実キー値は記録・転載しない
- 次のセキュリティ修正で、認証必須化とsecret非返却を確認する

次の工程は、まずAPIキーhardeningを実装・検証し、その合格後に今回のhybrid実装と検証結果をWindows v1.2.0最終ビルドへ反映し、クリーン管理者検証を行うこと。
