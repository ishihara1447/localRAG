# Linux（RHEL 9 / Docker / GPU）向けオフライン配布物の新規作成 — 報告

作成日: 2026-07-28
担当: Claude Code（サブエージェント）
対象: `linux-native/` 一式、`docs/LINUX_DISTRIBUTION_2026-07-28.md`（本書）
きっかけ: 導入先が Windows ではなく **RHEL 9 + Docker + NVIDIA GPU + 完全オフライン**であることが判明

> **⚠️ 最初に読むところ**
> - **Windows 資材（`windows-native/`, v1.2.7）は一切変更していない。** 削除も改変もしていない。
> - **`install.sh` は実行して検証していない。** 開発機では 166問のベースライン測定が
>   `anythingllm` / `rag-ollama` コンテナで走っており、コンテナの起動・停止ができないため。
>   何を実測し、何が未検証かは §6 に分けて書いた。
> - 本書で「推測」と書いた箇所は実測していない。断定はしていない。

---

## 0. 結論（3行）

1. `linux-native/` に **RHEL 9 向けオフライン配布物一式**を新規作成した。中核は
   SELinux ラベル `:z` 対応の compose、9段階の前提条件検査を持つ `install.sh`、
   ビルド時に走る2つの機械検証（イメージ内容の SHA-256 一致／同梱物の網羅性）。
2. **現行ソース（fork HEAD `48c68662`）から新イメージ `localrag-anythingllm:1.1.0` をビルド**し、
   イメージ内のファイルが fork のソースと SHA-256 一致することを実測で確認した。
   既存の `1.0.7`（5日前ビルド）にはオフライン強制・設定メニューガード・
   チャンク単位リランクが**入っていない**ため作り直しが必要だった。
3. **Windows 版 v1.2.7 の設定に2つの欠落を発見した**（`OLLAMA_DISABLE_THINKING` /
   `QUERY_REFORMULATION`）。どちらもコード側の既定が OFF で、採用済みの改善が
   Windows 配布では効いていない可能性が高い。Linux 版では入れてある。詳細 §4.2。

---

## 1. 成果物

（内容は §7 の実測値で確定）

---

## 2. 同梱物の実測サイズと内訳

（§7 で埋める）

---

## 3. Windows 版との差分

---

## 4. 設定値の移植

---

## 5. RHEL 9 特有の落とし穴への対処

---

## 6. 検証（実測できたこと／できなかったこと）

---

## 7. 導入先に確認が必要な残項目

