# シャード統合時の申し送り（Claude Code 本体が検証して記録）

## 検証済み（統合の前提として確定）

| 検証項目 | 結果 |
|---|---|
| **`phys` と投入済みドキュメントの `\f<n>\f` マーカーの対応** | **6ページ（39/98/263/391/477/519）で全件一致 ✅**。`phys`（pypdf物理ページ）＝マーカー番号。C4担当が「統合前に実測してほしい」と挙げた懸念は解消 |
| `physical_pages` 544 vs pypdf 546 | **フィクスチャが正しい**。546 − 1（p43白紙）− 1（p546裏表紙・p1と重複）= 544。詳細は `QUALITY_ROADMAP` §2.5.20 |
| アンカーの実在（C5_C6 前半14問） | **42件すべて原典に実在 ✅**（本体が pypdf で独立検証） |
| 既存20問のgoldページとの重複 | **全シャードで 0 ✅** |

## 🔴 統合時に対処が必要

### 1. 要素の重複（トピック集中）— **3シャード揃った時点で再計測（2026-07-28）**

**必須要素を2個以上共有する設問ペア＝9組**（定型句 `記載がありません` と単独の年月は除外して計測）。**うち8組が C2 × C4**。

| ペア | 共有している必須要素 |
|---|---|
| C2-007 × C4-020 | `協力支援活動` `国際平和協力業務` `国際緊急援助活動` `本来任務` |
| C2-008 × C4-029 | `2030年代` `極超音速滑空兵器` |
| C2-010 × C4-027 | `GIGO` `岡真臣` |
| C2-026 × C4-031 | `12物資` `経済安全保障推進法` |
| C2-028 × C4-035 | `13種目` `7名` `自衛隊体育学校` |
| C2-029 × C4-025 | `1,249個` `1,273件` |
| C2-042 × C4-030 | `15品目` `42品目` |
| C2-050 × C4-034 | `2024年11月9日` `朝霞訓練場` |
| C4-027 × C5-012 | `GIGO` `日英伊` |

1個だけ共有は15組（**許容範囲**）。全112問の必須要素（定型・年月を除く）は341個なので、**割合としては小さい**（現行20問は3トピックに9問=45%集中していた）。

**原因**: C2 と C4 は別担当が**並行作成**し、互いの成果を見られなかった。同じ素材に「2箇所を繋ぐ設問」と「要点をまとめる設問」を独立に作ると必須要素が重なる。**カテゴリ分割による並列化の構造的な副作用**であり、担当の落ち度ではない。

**⚠️ 旧記載（2シャード時点の3件）は上記に置き換わった。**

### 1-b. 旧記載（2シャード時点・参考）

C4 と C5 が**同一ページで同一の必須要素**を持つ組が2件ある。素材サーベイが警告した「**McNemarの対の独立性を損なう**」に該当する。

| 組 | 重複している必須要素 |
|---|---|
| **C5-012 × C4-027**（phys 484） | `GIGO`、`日英伊` |
| **C5-014 × C4-032**（phys 513） | `10の具体的な施策` |
| C5-013 × C4-026（phys 477） | なし ✅ |

**対処案**（統合時に選ぶ）:
- (a) 重複する要素を片方から外す（**分母が減るので、外した側の他要素で補う**）
- (b) 一方の設問を差し替える（素材は潤沢なので可能）
- (c) 重複を許容し、**統計処理で同一ページ由来の設問をクラスタとして扱う**

**(a) は「分母が減って点が上がる」方向なので、採用するなら必ず補充とセットにすること。**

### 2. 他シャード（C2 / C3+C1）との突き合わせが未実施

本ファイル作成時点で C2・C3+C1 は作成中。**全4シャードが揃ってから、同じ検証をやり直すこと。**

## 各シャードの実績

| シャード | 設問 | 要素（決定的） | 使用ページ |
|---|---|---|---|
| `C5_C6.json` | 27（C5 15 / C6 12） | 160（117） | 38 |
| `C4_summary.json` | 35 | 210（175） | 36 |

## 各担当が報告した注意点

- **`normalize()` は 兆/億/万 を数字に展開する**（`8兆4,748億円` → `8474800000000円`）。`[X]` 正規表現に `兆|億` を必須で書くと決定的に一致しない（C5_C6担当が2件修正済み）
- **`not_preceded_by` は前方しか見ない**。`インド ⊂ インド太平洋` のような後方包含は alias 側を後続込みにして回避する必要がある（C4担当の対処法）
- 素材サーベイの候補「女性自衛官比率の数値目標が無い」は**事実誤認**。phys 459 に「12%以上（2030年度まで）」が実在するため C6 として不成立（C5_C6担当が発見・代替3件を発掘）
